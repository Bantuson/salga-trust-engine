---
phase: 06.3-complete-ui-redesign
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - frontend-public/src/App.css
autonomous: true

must_haves:
  truths:
    - "All public dashboard pages use pink/rose background — no navy remnants"
    - "Header scrolled state uses pink-tinted glassmorphism instead of navy"
    - "Footer uses pink surface tones instead of navy"
    - "All accent colors (links, CTAs, decorative elements) read well on pink background"
    - "Mobile menu overlay uses pink-tinted backdrop instead of navy"
  artifacts:
    - path: "frontend-public/src/App.css"
      provides: "Complete pink/rose re-skin for all public pages"
  key_links:
    - from: "frontend-public/src/App.css"
      to: "shared/design-tokens.css"
      via: "@import"
      pattern: "var\\(--"
---

<objective>
Re-skin the entire frontend-public App.css to replace all remaining hardcoded navy colors and navy-era accent references with the pink/rose theme.

Purpose: While design-tokens.css cascades most token changes automatically, App.css contains many hardcoded rgba() values, gradient backgrounds, and navy-specific overrides that must be manually updated. This completes the public dashboard visual identity.

Output: Fully pink/rose themed public dashboard CSS with no navy color remnants.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-complete-ui-redesign-with-johannesburg-skyline-background-glassmorphic-cards-pink-rose-theme-and-proper-design-system-for-both-dashboards/06.3-RESEARCH.md
@frontend-public/src/App.css
@.planning/phases/06.3-complete-ui-redesign-with-johannesburg-skyline-background-glassmorphic-cards-pink-rose-theme-and-proper-design-system-for-both-dashboards/06.3-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin header, footer, and mobile menu for pink/rose theme</name>
  <files>frontend-public/src/App.css</files>
  <action>
Update these specific sections in frontend-public/src/App.css:

**Public Header (.public-header):**
- `.public-header` — change `background-color: rgba(10, 14, 26, 0)` to `background-color: rgba(205, 94, 129, 0)` (transparent pink start)
- `.public-header.scrolled` — change `background-color: rgba(10, 14, 26, 0.95)` to `background-color: rgba(163, 72, 102, 0.92)` (deep rose scrolled state, slightly translucent for glassmorphic feel); add `backdrop-filter: blur(10px)` (already present, keep it)
- `.logo-text` — change gradient from `var(--color-coral), var(--color-teal)` to `var(--text-primary), var(--color-accent-gold)` (light-to-gold gradient on pink)
- `.nav-link.active::after` — change `background: var(--color-coral)` to `background: var(--color-accent-gold)` (gold underline)
- `.nav-link:hover` color stays `var(--text-primary)` (works on pink)

**Mobile Menu Overlay (.mobile-menu-overlay):**
- Change `background: rgba(10, 14, 26, 0.98)` to `background: rgba(163, 72, 102, 0.96)` (deep rose overlay)
- `.mobile-nav-link:hover, .mobile-nav-link.active` — change `color: var(--color-coral)` to `color: var(--color-accent-gold)`

**Footer (.public-footer, .site-footer):**
- `.site-footer` — change `background: var(--color-navy-light)` to `background: rgba(163, 72, 102, 0.6)` (deeper rose, distinguishes from body)
- `.public-footer` — background already uses `var(--surface-elevated)` which will cascade correctly
- `.emergency-number` — change `color: var(--color-coral)` to `color: var(--color-accent-gold)` and hover to `color: var(--color-teal)`
- `.footer-link:hover` — change `color: var(--color-coral)` to `color: var(--color-accent-gold)`

**Accent color text class:**
- `.text-coral` — update gradient: `background: linear-gradient(135deg, var(--color-accent-gold), var(--color-teal))` (gold-to-teal gradient text, reads well on pink)
  </action>
  <verify>
1. `grep "0A0E1A\|10, 14, 26" frontend-public/src/App.css` returns zero matches
2. Header scrolled state uses rose rgba values
3. Footer uses rose/pink tones
  </verify>
  <done>Header, footer, and mobile menu are fully pink/rose themed with no navy rgba values.</done>
</task>

<task type="auto">
  <name>Task 2: Re-skin all landing page sections and remaining pages for pink/rose</name>
  <files>frontend-public/src/App.css</files>
  <action>
Continue updating frontend-public/src/App.css for all remaining sections:

**Solution Section (.solution-section):**
- Change `background: linear-gradient(135deg, rgba(255, 107, 74, 0.05), rgba(0, 217, 166, 0.05))` to `background: linear-gradient(135deg, rgba(255, 213, 79, 0.08), rgba(0, 191, 165, 0.08))` (gold/teal subtle gradient on pink)
- `.solution-list li::before` — change `color: var(--color-coral)` to `color: var(--color-accent-gold)`

**Features Section:**
- `.card-glow-coral` → rename to `.card-glow-gold`, update `border-color: var(--glow-gold-border)` and `:hover box-shadow: var(--glow-gold)`
- `.feature-icon` — change `color: var(--color-coral)` to `color: var(--color-accent-gold)`

**Live Stats Section (.live-stats-section):**
- Change `background: linear-gradient(135deg, rgba(0, 217, 166, 0.1), transparent)` to `background: linear-gradient(135deg, rgba(0, 191, 165, 0.1), transparent)` (teal tint on pink)

**Dashboard Section (.dashboard-section):**
- Change `background: var(--color-navy-light)` to `background: var(--color-rose-deep)` or `rgba(163, 72, 102, 0.5)`

**Dashboard Preview Section:**
- Change the `linear-gradient(135deg, rgba(255, 107, 74, 0.05), rgba(0, 217, 166, 0.05))` to `linear-gradient(135deg, rgba(255, 213, 79, 0.08), rgba(0, 191, 165, 0.08))`

**CTA buttons:**
- `.hero-cta` — change `background: var(--color-coral)` to `background: var(--color-accent-gold)`; `color: #333` (dark text on gold button); hover to `var(--color-accent-gold-hover)` with gold glow shadow
- `.preview-cta-button` — same gold button treatment
- `.hero-cta:hover` shadow — change coral rgba to gold rgba: `0 0 30px rgba(255, 213, 79, 0.35), 0 0 80px rgba(255, 213, 79, 0.12)`

**About Page (.about-page):**
- `.about-hero` — change gradient from coral/teal to gold/teal: `linear-gradient(135deg, rgba(255, 213, 79, 0.08), rgba(0, 191, 165, 0.08))`
- `.about-content li::before` — keep `color: var(--color-teal)` (already works on pink)

**Report Redirect Page:**
- `.report-info-box li::before` — change `color: var(--color-coral)` to `color: var(--color-accent-gold)`

**Citizen Portal:**
- `.filter-tab:hover` — change `background: rgba(0, 217, 166, 0.1)` to `rgba(0, 191, 165, 0.15)` and `color: var(--color-teal)` (stays)
- `.gbv-emergency-section` — change `rgba(255, 107, 74, ...)` to `rgba(255, 213, 79, ...)` for gold emergency accent on pink

**Heatmap:**
- `.heatmap-range-slider` thumb — keep `var(--color-teal)` (works on pink); adjust glow to teal rgba

**Focus/error states:**
- `input:focus` — keep `var(--color-teal)` and teal box-shadow (works on pink)
- `input.error` — change `var(--color-coral)` to a warm red that reads on pink: keep coral `#FF6B4A` or shift to `#ff5252` — use `rgba(255, 82, 82, 0.15)` for box-shadow
- `button:focus-visible` — keep `outline: 2px solid var(--color-teal)` (works on pink)

**Card glow classes — add .card-glow-gold, keep .card-glow-teal, remove old .card-glow-coral:**

Do NOT change any spacing, layout, grid, or responsive breakpoints — this is colors only.
  </action>
  <verify>
1. `grep "color-coral\|FF6B4A\|255, 107, 74\|color-navy\|color-navy-light" frontend-public/src/App.css` — should return zero or near-zero matches (some may remain in error states if coral is kept as error color)
2. All section backgrounds use pink/rose/gold/teal tones
3. CTA buttons use gold instead of coral
  </verify>
  <done>All public App.css sections use pink/rose theme. No navy colors, coral accent replaced with gold. Every section background, button, link, and decoration matches the new identity.</done>
</task>

</tasks>

<verification>
After both tasks:
1. Full search: `grep -c "0A0E1A\|131829\|color-navy" frontend-public/src/App.css` returns 0
2. No hardcoded `rgba(10, 14, 26, ...)` values remain
3. Buttons use gold accent, links use gold hover, teal kept for interactive elements
4. Header/footer/sections all visually consistent on pink background
</verification>

<success_criteria>
- Zero navy color references in frontend-public/src/App.css
- Header scrolled state is pink glassmorphic
- Footer is pink-toned
- All CTAs use gold accent
- All section gradient backgrounds use gold/teal-on-pink
- Mobile menu overlay is deep rose
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-complete-ui-redesign-with-johannesburg-skyline-background-glassmorphic-cards-pink-rose-theme-and-proper-design-system-for-both-dashboards/06.3-04-SUMMARY.md`
</output>
