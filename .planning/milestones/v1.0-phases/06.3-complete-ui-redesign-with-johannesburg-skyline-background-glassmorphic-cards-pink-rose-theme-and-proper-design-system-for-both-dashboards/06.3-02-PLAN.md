---
phase: 06.3-complete-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend-public/src/components/Globe3D.tsx
  - frontend-public/src/components/GlobeLoadingSkeleton.tsx
  - frontend-public/src/components/landing/HeroSection.tsx
  - frontend-public/src/pages/TransparencyDashboardPage.tsx
  - frontend-public/src/components/landing/FeaturesSection.tsx
  - frontend-public/src/components/landing/LiveStatsSection.tsx
  - frontend-public/src/components/layout/PublicFooter.tsx
  - frontend-public/src/pages/AboutPage.tsx
  - frontend-public/src/pages/CitizenPortalPage.tsx
  - frontend-dashboard/src/components/Globe3DSmall.tsx
  - frontend-dashboard/src/pages/LoginPage.tsx
  - frontend-dashboard/src/pages/RegisterPage.tsx
  - shared/components/NdebelePattern.tsx
autonomous: true

must_haves:
  truths:
    - "No Globe3D, Globe3DSmall, or GlobeLoadingSkeleton components are imported or rendered anywhere"
    - "No NdebelePattern component is imported or rendered anywhere"
    - "Both frontend apps build without errors after removal"
  artifacts:
    - path: "frontend-public/src/components/landing/HeroSection.tsx"
      provides: "Hero section without globe"
    - path: "frontend-public/src/pages/TransparencyDashboardPage.tsx"
      provides: "Dashboard page without globe or Ndebele patterns"
    - path: "frontend-dashboard/src/pages/LoginPage.tsx"
      provides: "Login page without globe"
  key_links:
    - from: "HeroSection.tsx"
      to: "LandingPage"
      via: "component import"
      pattern: "HeroSection"
---

<objective>
Remove all Globe3D and NdebelePattern components from both dashboards.

Purpose: Per user decision, the 3D globe is removed entirely (clashes with skyline background) and Ndebele/Zulu geometric patterns are removed entirely (skyline + pink identity is sufficient). This cleanup must happen before adding skyline imagery.

Output: Both apps build cleanly with no dead globe/pattern imports, no React Three Fiber lazy-load errors.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-complete-ui-redesign-with-johannesburg-skyline-background-glassmorphic-cards-pink-rose-theme-and-proper-design-system-for-both-dashboards/06.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Globe3D from both dashboards</name>
  <files>
    frontend-public/src/components/landing/HeroSection.tsx
    frontend-public/src/pages/TransparencyDashboardPage.tsx
    frontend-dashboard/src/pages/LoginPage.tsx
    frontend-dashboard/src/pages/RegisterPage.tsx
  </files>
  <action>
**frontend-public/src/components/landing/HeroSection.tsx:**
- Remove: `import { GlobeLoadingSkeleton } from '../GlobeLoadingSkeleton';`
- Remove: `const Globe3D = React.lazy(() => import('../Globe3D'));`
- Remove the `globeRef` useRef and any GSAP parallax animation targeting the globe
- Remove the entire `<div ref={globeRef} className="hero-globe-container">...</div>` block containing the Suspense/Globe3D
- Keep the section structure, hero-content, hero-headline, hero-subtitle, and hero-gradient-overlay intact
- The hero section will temporarily show just the background color (plan 03 adds skyline)

**frontend-public/src/pages/TransparencyDashboardPage.tsx:**
- Remove: `import { GlobeLoadingSkeleton } from '../components/GlobeLoadingSkeleton';`
- Remove: `const Globe3D = lazy(() => import('../components/Globe3D'));`
- Remove the `<div className="dashboard-header-globe">...</div>` block with Suspense/Globe3D
- Keep dashboard-header structure, dashboard-header-content, title, subtitle

**frontend-dashboard/src/pages/LoginPage.tsx:**
- Remove: `const Globe3DSmall = lazy(() => import('../components/Globe3DSmall'));`
- Remove the `<Suspense fallback={...}><Globe3DSmall /></Suspense>` block
- If `Suspense` and `lazy` imports are only used for globe, remove them from the import statement
- Keep the login form, AnimatedGradientBg, all auth functionality unchanged

**frontend-dashboard/src/pages/RegisterPage.tsx:**
- Remove: `const Globe3DSmall = lazy(() => import('../components/Globe3DSmall'));`
- Remove the `<Suspense fallback={...}><Globe3DSmall /></Suspense>` block
- If `Suspense` and `lazy` imports are only used for globe, remove them from the import statement
- Keep the registration form, AnimatedGradientBg, all auth functionality unchanged

**DELETE these files entirely:**
- `frontend-public/src/components/Globe3D.tsx`
- `frontend-public/src/components/GlobeLoadingSkeleton.tsx`
- `frontend-dashboard/src/components/Globe3DSmall.tsx`
  </action>
  <verify>
Run: `grep -r "Globe3D\|GlobeLoadingSkeleton" frontend-public/src/ frontend-dashboard/src/` — should return zero matches.
Verify deleted files: `ls frontend-public/src/components/Globe3D.tsx` should fail (not found).
  </verify>
  <done>All Globe3D, Globe3DSmall, and GlobeLoadingSkeleton references are removed from both apps. Three component files are deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Remove NdebelePattern from all pages and delete component</name>
  <files>
    frontend-public/src/pages/TransparencyDashboardPage.tsx
    frontend-public/src/pages/AboutPage.tsx
    frontend-public/src/pages/CitizenPortalPage.tsx
    frontend-public/src/components/landing/FeaturesSection.tsx
    frontend-public/src/components/landing/LiveStatsSection.tsx
    frontend-public/src/components/layout/PublicFooter.tsx
    shared/components/NdebelePattern.tsx
    frontend-public/src/App.css
  </files>
  <action>
**Remove NdebelePattern import and usage from each file:**

1. **TransparencyDashboardPage.tsx**: Remove `import { NdebelePattern }` and all `<NdebelePattern ... />` JSX elements
2. **AboutPage.tsx**: Remove `import { NdebelePattern }` and all `<NdebelePattern variant="border" />` JSX elements (3 usages)
3. **CitizenPortalPage.tsx**: Remove `import { NdebelePattern }` and the `<NdebelePattern variant="background" opacity={0.05} />` element
4. **FeaturesSection.tsx**: Remove `import { NdebelePattern }` and all `<NdebelePattern variant="corner" opacity={0.08} />` elements (3 usages)
5. **LiveStatsSection.tsx**: Remove `import { NdebelePattern }` and both `<NdebelePattern variant="border" opacity={0.15} />` elements
6. **PublicFooter.tsx**: Remove `import { NdebelePattern }` and the `<NdebelePattern variant="border" className="footer-decoration" />` element

**DELETE the component file:**
- `shared/components/NdebelePattern.tsx`

**frontend-public/src/App.css:**
- Remove the `.ndebele-pattern { display: none !important; }` rule inside the mobile media query (around line 585) since the component no longer exists

Ensure no empty wrappers are left behind — if a NdebelePattern was the only child of a wrapper div, remove the wrapper too.
  </action>
  <verify>
Run: `grep -r "NdebelePattern\|ndebele" frontend-public/src/ frontend-dashboard/src/ shared/` — should return zero matches.
Verify deleted: `ls shared/components/NdebelePattern.tsx` should fail (not found).
  </verify>
  <done>NdebelePattern component is deleted and all imports/usages removed from both dashboards. No Ndebele references remain anywhere.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -r "Globe3D\|GlobeLoadingSkeleton\|Globe3DSmall" frontend-public/ frontend-dashboard/` — zero matches
2. `grep -r "NdebelePattern\|ndebele" frontend-public/ frontend-dashboard/ shared/` — zero matches
3. Verify no orphaned imports (Suspense/lazy used only for globe)
4. Both apps should have no TypeScript import errors for removed modules
</verification>

<success_criteria>
- Globe3D.tsx, Globe3DSmall.tsx, GlobeLoadingSkeleton.tsx are deleted
- NdebelePattern.tsx is deleted
- Zero import references to deleted components in either app
- HeroSection, TransparencyDashboardPage, LoginPage, RegisterPage, AboutPage, CitizenPortalPage, FeaturesSection, LiveStatsSection, PublicFooter are all updated
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-complete-ui-redesign-with-johannesburg-skyline-background-glassmorphic-cards-pink-rose-theme-and-proper-design-system-for-both-dashboards/06.3-02-SUMMARY.md`
</output>
