---
phase: 34.2-dropdown-menu-styling-across-all-roles-and-pages
plan: 03
type: execute
wave: 2
depends_on: ["34.2-01"]
files_modified:
  - frontend-dashboard/src/pages/AccessRequestsPage.tsx
  - frontend-dashboard/src/pages/ActualsPage.tsx
  - frontend-dashboard/src/pages/IdpDetailPage.tsx
  - frontend-dashboard/src/pages/OnboardingWizardPage.tsx
  - frontend-dashboard/src/pages/OversightDashboardPage.tsx
  - frontend-dashboard/src/pages/PmsSetupWizardPage.tsx
  - frontend-dashboard/src/pages/RequestAccessPage.tsx
  - frontend-dashboard/src/pages/RoleApprovalsPage.tsx
  - frontend-dashboard/src/pages/SdbipKpiPage.tsx
  - frontend-public/src/pages/ProfilePage.tsx
  - frontend-public/src/pages/ReportIssuePage.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 9 page files in frontend-dashboard use unified Select instead of native <select>"
    - "Both frontend-public page files use unified Select instead of native <select>"
    - "No native <select> elements remain anywhere in either dashboard's pages directories"
    - "PmsSetupWizardPage parseInt conversions preserved correctly in onChange handlers"
    - "ReportIssuePage GBV trigger logic preserved during migration"
    - "ProfilePage disabled state preserved on municipality picker"
    - "TypeScript build passes in both frontend-dashboard and frontend-public"
  artifacts:
    - path: "frontend-dashboard/src/pages/PmsSetupWizardPage.tsx"
      provides: "Migrated PMS setup selects with parseInt conversions"
      contains: "import.*Select.*from.*@shared/components/ui/Select"
    - path: "frontend-public/src/pages/ReportIssuePage.tsx"
      provides: "Migrated category picker preserving GBV trigger logic"
      contains: "import.*Select.*from.*@shared/components/ui/Select"
    - path: "frontend-public/src/pages/ProfilePage.tsx"
      provides: "Migrated municipality picker with disabled state"
      contains: "import.*Select.*from.*@shared/components/ui/Select"
  key_links:
    - from: "frontend-dashboard/src/pages/*.tsx"
      to: "@shared/components/ui/Select"
      via: "import { Select }"
      pattern: "import.*Select.*from.*@shared/components/ui/Select"
    - from: "frontend-public/src/pages/*.tsx"
      to: "@shared/components/ui/Select"
      via: "import { Select }"
      pattern: "import.*Select.*from.*@shared/components/ui/Select"
---

<objective>
Migrate all 9 page files in frontend-dashboard and 2 page files in frontend-public from native `<select>` elements to the unified glass-styled `Select` component.

Purpose: Eliminates the last native OS-chrome select dropdowns from both dashboards, completing the full migration. Special attention to PmsSetupWizardPage (5 selects with parseInt conversions), ReportIssuePage (GBV trigger logic), and ProfilePage (disabled municipality picker).

Output: 11 page files migrated, zero native `<select>` elements remaining in either dashboard.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34.2-dropdown-menu-styling-across-all-roles-and-pages/34.2-CONTEXT.md
@.planning/phases/34.2-dropdown-menu-styling-across-all-roles-and-pages/34.2-RESEARCH.md
@.planning/phases/34.2-dropdown-menu-styling-across-all-roles-and-pages/34.2-01-SUMMARY.md

<interfaces>
<!-- Unified Select API from Plan 01 -->
From shared/components/ui/Select.tsx:
```typescript
export interface SelectOption {
  value: string;
  label: string;
}

interface SelectProps {
  options: SelectOption[];
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  label?: string;
  disabled?: boolean;
  required?: boolean;
  fullWidth?: boolean;       // default true
  size?: 'sm' | 'md' | 'lg'; // default 'md'
  ariaLabel?: string;
  autoFlip?: boolean;        // default false
}

export function Select(props: SelectProps): JSX.Element;
```
</interfaces>

<migration_pattern>
## Standard Migration Pattern

Every native `<select>` migration follows this formula:

**Step 1: Add import**
```tsx
import { Select } from '@shared/components/ui/Select';
```

**Step 2: Replace JSX**
```tsx
// BEFORE
<select
  value={stateValue}
  onChange={(e) => handler(e.target.value)}
  style={{ ... }}
>
  <option value="">All</option>
  <option value="open">Open</option>
</select>

// AFTER
<Select
  value={stateValue}
  onChange={(value) => handler(value)}
  options={[
    { value: '', label: 'All' },
    { value: 'open', label: 'Open' },
  ]}
  size="md"
/>
```

**Key deltas:**
- `onChange`: `(e) => fn(e.target.value)` becomes `(value) => fn(value)`
- `<option>` children become `options` array prop
- Native `style` prop on `<select>` is removed
- For typed casts: `e.target.value as SomeType` becomes `value as SomeType`
- For parseInt: `parseInt(e.target.value, 10)` becomes `parseInt(value, 10)`
- For disabled selects: `disabled` prop is the same on both
- Select value prop is ALWAYS string — numeric state must be stringified via String(numericValue) for value prop
</migration_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate dashboard pages — AccessRequestsPage, ActualsPage, IdpDetailPage, OnboardingWizardPage, OversightDashboardPage</name>
  <files>
    frontend-dashboard/src/pages/AccessRequestsPage.tsx,
    frontend-dashboard/src/pages/ActualsPage.tsx,
    frontend-dashboard/src/pages/IdpDetailPage.tsx,
    frontend-dashboard/src/pages/OnboardingWizardPage.tsx,
    frontend-dashboard/src/pages/OversightDashboardPage.tsx
  </files>
  <action>
For each of the 5 files below, read the file, find every native `<select>` element, and apply the standard migration pattern.

**AccessRequestsPage.tsx** — Has 2 native `<select>` elements (~lines 372, 387):
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert both selects (likely status and role filters) to `<Select options={[...]} />`
- Change onChange handlers from `(e) => ...e.target.value...` to `(value) => ...value...`
- Remove native select `style` props

**ActualsPage.tsx** — Has 1 native `<select>` (~line 329):
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert to Select with options array
- Apply standard migration pattern

**IdpDetailPage.tsx** — Has 1 native `<select>` (~line 410):
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert to Select with options array
- Apply standard migration pattern

**OnboardingWizardPage.tsx** — Has 2 native `<select>` elements (~lines 946, 1364):
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert both selects to Select with options arrays
- These are likely step-specific configuration selects — preserve all conditional rendering logic around them
- Apply standard migration pattern

**OversightDashboardPage.tsx** — Has 1 native `<select>` (~line 902):
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert to Select with options array (likely a filter select)
- Apply standard migration pattern

**For all files:** Remove inline styles on native selects, keep wrapper/container styles, use `size="md"` for page-level selects.
  </action>
  <verify>
    <automated>cd frontend-dashboard && npm run build:check</automated>
  </verify>
  <done>
- All 5 page files import Select from @shared/components/ui/Select
- No native `<select>` elements remain in these files
- onChange handlers use direct value pattern
- TypeScript build passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate PmsSetupWizardPage, RequestAccessPage, RoleApprovalsPage, SdbipKpiPage, and frontend-public pages</name>
  <files>
    frontend-dashboard/src/pages/PmsSetupWizardPage.tsx,
    frontend-dashboard/src/pages/RequestAccessPage.tsx,
    frontend-dashboard/src/pages/RoleApprovalsPage.tsx,
    frontend-dashboard/src/pages/SdbipKpiPage.tsx,
    frontend-public/src/pages/ProfilePage.tsx,
    frontend-public/src/pages/ReportIssuePage.tsx
  </files>
  <action>
For each of the 6 files below, read the file, find every native `<select>` element, and apply the standard migration pattern.

**PmsSetupWizardPage.tsx** — Has 5 native `<select>` elements (~lines 715, 739, 754, 845, 896) — HIGHEST COMPLEXITY:
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert all 5 selects to `<Select options={[...]} />`
- CRITICAL: This file has `parseInt(e.target.value, 10)` conversions for numeric settings (e.g., `sdbip_layers`). The migration pattern is:
  ```tsx
  // BEFORE
  <select value={settings.sdbip_layers} onChange={(e) => setSettings(s => ({ ...s, sdbip_layers: parseInt(e.target.value, 10) }))}>

  // AFTER
  <Select
    value={String(settings.sdbip_layers)}
    onChange={(value) => setSettings(s => ({ ...s, sdbip_layers: parseInt(value, 10) }))}
    options={SDBIP_LAYER_OPTIONS.map(n => ({ value: String(n), label: `${n} layer${n !== 1 ? 's' : ''}` }))}
    size="md"
  />
  ```
- Note: Select `value` prop is ALWAYS string. Use `String(numericValue)` for numeric state values.
- For options that are dynamically generated from arrays of numbers, map each to `{ value: String(n), label: '...' }`

**RequestAccessPage.tsx** — Has 2 native `<select>` elements (~lines 388, 415):
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert both selects (likely municipality and role selects)
- Apply standard migration pattern

**RoleApprovalsPage.tsx** — Has 3 native `<select>` elements (~lines 595, 612, 629):
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert all 3 filter selects to Select components
- Apply standard migration pattern

**SdbipKpiPage.tsx** — Has 1 native `<select>` (~line 278):
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert inline form select to Select component
- Apply standard migration pattern

**ProfilePage.tsx (frontend-public)** — Has 1 native `<select>` (~line 474, municipality picker):
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert municipality picker to Select component
- IMPORTANT: Preserve the `disabled` prop — the municipality picker is disabled when not in edit mode. The unified Select supports `disabled` prop natively.
- Apply standard migration pattern

**ReportIssuePage.tsx (frontend-public)** — Has 1 native `<select>` (~line 514, category picker):
- Add `import { Select } from '@shared/components/ui/Select'`
- Convert category picker to Select component
- IMPORTANT: Preserve the GBV trigger logic. The category select's onChange handler likely has special logic that triggers a GBV-related flow when a specific category is selected. Preserve this logic exactly — only change the event signature from `(e) => handler(e.target.value)` to `(value) => handler(value)`.
- Apply standard migration pattern

**For all files:** Remove inline styles on native selects, keep wrapper/container styles, use `size="md"` for all page-level selects.
  </action>
  <verify>
    <automated>cd frontend-dashboard && npm run build:check && cd ../frontend-public && npm run build:check</automated>
  </verify>
  <done>
- All 4 frontend-dashboard page files import Select from @shared/components/ui/Select
- Both frontend-public page files import Select from @shared/components/ui/Select
- PmsSetupWizardPage parseInt conversions preserved correctly (value not e.target.value)
- ProfilePage disabled prop preserved on municipality picker
- ReportIssuePage GBV trigger logic preserved
- No native `<select>` elements remain in any of these files
- TypeScript build passes in BOTH frontend-dashboard AND frontend-public with zero errors
  </done>
</task>

</tasks>

<verification>
1. `cd frontend-dashboard && npm run build:check` — zero TypeScript errors
2. `cd frontend-public && npm run build:check` — zero TypeScript errors
3. `grep -rl '<select' frontend-dashboard/src/pages/ --include='*.tsx'` — returns ZERO files
4. `grep -rl '<select' frontend-public/src/pages/ --include='*.tsx'` — returns ZERO files
5. Verify PmsSetupWizardPage: `grep -n 'parseInt' frontend-dashboard/src/pages/PmsSetupWizardPage.tsx` — parseInt calls still present with `value` instead of `e.target.value`
6. Verify ReportIssuePage: GBV category handling logic unchanged (only event signature changed)
</verification>

<success_criteria>
- All 9 page files in frontend-dashboard migrated from native `<select>` to unified Select
- Both frontend-public page files migrated from native `<select>` to unified Select
- All parseInt conversions in PmsSetupWizardPage preserved correctly
- GBV trigger logic in ReportIssuePage preserved
- Disabled state in ProfilePage municipality picker preserved
- TypeScript build clean in both dashboards
- Zero native `<select>` elements remain in either dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/34.2-dropdown-menu-styling-across-all-roles-and-pages/34.2-03-SUMMARY.md`
</output>
