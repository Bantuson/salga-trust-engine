---
phase: 34.2-dropdown-menu-styling-across-all-roles-and-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/components/ui/Select.tsx
  - frontend-dashboard/src/components/settings/AuditLogSection.tsx
  - frontend-dashboard/src/components/settings/TeamDefaultsSection.tsx
  - frontend-dashboard/src/components/rbac/RoleSwitcher.tsx
  - frontend-dashboard/src/components/settings/GlassSelect.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Unified Select component renders with GlassSelect's frosted glass styling (rgba(255,255,255,0.15) backdrop, blur(24px))"
    - "Select component supports size='sm', size='md', size='lg' presets with distinct padding/font sizes"
    - "Select dropdown panel has data-lenis-prevent to prevent scroll bleed"
    - "Keyboard navigation works: ArrowDown/ArrowUp to navigate, Enter/Space to select, Escape to close"
    - "RoleSwitcher uses unified Select with sm size, transparent container, full role labels"
    - "GlassSelect.tsx is deleted and no file imports it"
    - "AuditLogSection and TeamDefaultsSection import from @shared/components/ui/Select"
  artifacts:
    - path: "shared/components/ui/Select.tsx"
      provides: "Unified glass-styled Select component with size presets, autoFlip, ariaLabel, keyboard nav, ARIA"
      exports: ["Select", "SelectOption"]
    - path: "frontend-dashboard/src/components/rbac/RoleSwitcher.tsx"
      provides: "RoleSwitcher using unified Select sm size with transparent container"
  key_links:
    - from: "frontend-dashboard/src/components/settings/AuditLogSection.tsx"
      to: "@shared/components/ui/Select"
      via: "import { Select }"
      pattern: "import.*Select.*from.*@shared/components/ui/Select"
    - from: "frontend-dashboard/src/components/settings/TeamDefaultsSection.tsx"
      to: "@shared/components/ui/Select"
      via: "import { Select }"
      pattern: "import.*Select.*from.*@shared/components/ui/Select"
    - from: "frontend-dashboard/src/components/rbac/RoleSwitcher.tsx"
      to: "@shared/components/ui/Select"
      via: "import { Select }"
      pattern: "import.*Select.*from.*@shared/components/ui/Select"
---

<objective>
Upgrade the shared Select component to the unified glass-styled design and migrate the three special-case consumers (AuditLogSection, TeamDefaultsSection, RoleSwitcher), then delete GlassSelect.tsx.

Purpose: Establishes the single canonical Select component that all subsequent migration plans depend on. The unified component merges GlassSelect's frosted glass aesthetic with Select.tsx's keyboard navigation and ARIA attributes, adding size presets and autoFlip support.

Output: Upgraded `shared/components/ui/Select.tsx` with glass styling + size presets, three migrated consumers, GlassSelect.tsx deleted.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.2-dropdown-menu-styling-across-all-roles-and-pages/34.2-CONTEXT.md
@.planning/phases/34.2-dropdown-menu-styling-across-all-roles-and-pages/34.2-RESEARCH.md

@shared/components/ui/Select.tsx
@frontend-dashboard/src/components/settings/GlassSelect.tsx
@frontend-dashboard/src/components/rbac/RoleSwitcher.tsx
@frontend-dashboard/src/components/settings/AuditLogSection.tsx
@frontend-dashboard/src/components/settings/TeamDefaultsSection.tsx
@shared/design-tokens.css

<interfaces>
<!-- Current shared Select API (will be upgraded in place) -->
From shared/components/ui/Select.tsx:
```typescript
export interface SelectOption {
  value: string;
  label: string;
}

interface SelectProps {
  options: SelectOption[];
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  label?: string;
  disabled?: boolean;
  required?: boolean;
  fullWidth?: boolean;
}

export function Select(props: SelectProps): JSX.Element;
```

From frontend-dashboard/src/components/settings/GlassSelect.tsx:
```typescript
interface GlassSelectProps {
  value: string;
  onChange: (value: string) => void;
  options: Option[];
  ariaLabel?: string;
  id?: string;
  maxWidth?: string;
}

export function GlassSelect(props: GlassSelectProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade shared Select.tsx to unified glass-styled component</name>
  <files>shared/components/ui/Select.tsx</files>
  <action>
Rewrite `shared/components/ui/Select.tsx` IN PLACE. Keep the existing `SelectOption` export interface and `Select` export function name. Merge GlassSelect's glass styling with the existing keyboard navigation and ARIA attributes.

**New props to add (extend `SelectProps`):**
- `size?: 'sm' | 'md' | 'lg'` — defaults to `'md'`
- `ariaLabel?: string` — sets `aria-label` on the trigger button (for selects without visible labels)
- `autoFlip?: boolean` — defaults to `false`; when true, detects available space below trigger and flips dropdown upward if insufficient room

**Size preset constants (SIZE_STYLES object):**
```
sm: { padding: '6px 10px', fontSize: 'var(--text-xs)' }
md: { padding: '8px 2.5rem 8px 12px', fontSize: 'var(--text-sm)' }
lg: { padding: '12px 2.5rem 12px 16px', fontSize: 'var(--text-base)' }
```

**Trigger button styling changes (replace current styles):**
- background: `rgba(255, 255, 255, 0.12)` (from GlassSelect, replaces `var(--surface-elevated)`)
- border: `1px solid rgba(255, 255, 255, 0.2)` when closed, `1px solid var(--color-teal)` when open
- borderRadius: `var(--radius-sm)` (from GlassSelect, replaces `var(--radius-md)`)
- boxShadow: REMOVE entirely (was `0 0 0 2px rgba(0, 191, 165, 0.15)` when open — per locked decision NO box-shadow glow)
- transition: `border-color 0.2s` (remove `, box-shadow 0.2s`)
- Chevron SVG: change stroke color from `%23ffd54f` (gold) to `%23c7b3ba` (var(--text-muted) hex value)
- Apply SIZE_STYLES[size] padding and fontSize to the trigger button
- For `sm` size: right padding in the trigger should accommodate chevron without the 2.5rem reserved space — use `28px` right padding for sm
- Add textOverflow: 'ellipsis', overflow: 'hidden', whiteSpace: 'nowrap' to trigger text content

**Dropdown panel styling changes (replace current styles):**
- background: `rgba(255, 255, 255, 0.15)` (from GlassSelect, replaces `rgba(30, 20, 40, 0.92)`)
- border: `1px solid rgba(255, 255, 255, 0.25)` (from GlassSelect)
- borderRadius: `var(--radius-sm)` (from GlassSelect)
- boxShadow: `0 8px 32px rgba(0, 0, 0, 0.3)` (from GlassSelect)
- maxHeight: `240px` (from GlassSelect, replaces `280px`)
- Add `data-lenis-prevent` attribute to the dropdown panel div
- Add `padding: '4px'` to the dropdown panel (from GlassSelect)
- backdropFilter and WebkitBackdropFilter stay at `blur(24px)` (already correct)

**Option item styling:**
- Keep existing: hover → `rgba(205, 94, 129, 0.18)` (already correct)
- Selected item: color `var(--color-teal)`, background `rgba(0, 191, 165, 0.25)` — add the background highlight for selected items (currently only changes color, not background)
- Add `borderRadius: '4px'` to each option (from GlassSelect)
- Add `whiteSpace: 'nowrap'` to each option (from GlassSelect)

**Auto-flip logic (when autoFlip={true}):**
- Add state: `const [dropUp, setDropUp] = useState(false);`
- In the toggle click handler, BEFORE `setIsOpen(!isOpen)`:
  ```
  if (!isOpen && autoFlip && wrapperRef.current) {
    const rect = wrapperRef.current.getBoundingClientRect();
    const spaceBelow = window.innerHeight - rect.bottom;
    setDropUp(spaceBelow < 248); // 240px maxHeight + 8px buffer
  }
  ```
- Dropdown panel position: when `dropUp` is true, set `bottom: 'calc(100% + 4px)'` and remove `top`; when false, keep current `top: 'calc(100% + 4px)'`

**Keep unchanged:**
- Click-outside handler (useEffect with mousedown)
- Scroll focused item into view (useEffect with scrollIntoView)
- handleKeyDown callback (all keyboard navigation logic)
- ARIA attributes (aria-haspopup, aria-expanded, role="listbox", role="option", aria-selected)
- The allOptions prepend-placeholder pattern
- fullWidth prop behavior
- label rendering
- disabled/required behavior
  </action>
  <verify>
    <automated>cd frontend-dashboard && npm run build:check</automated>
  </verify>
  <done>
- shared/components/ui/Select.tsx exports `Select` and `SelectOption` with new props: size, ariaLabel, autoFlip
- Glass styling applied: rgba(255,255,255,0.15) panel background, blur(24px), muted chevron (%23c7b3ba)
- No box-shadow glow on trigger when open
- data-lenis-prevent on dropdown panel div
- Size presets: sm/md/lg with correct padding and font sizes
- autoFlip logic: detects space below and flips dropdown upward when insufficient room
- All 5 existing consumers (GoldenThreadPage, PerformanceAgreementsPage, PmsHubPage, SdbipPage, StatutoryReportsPage) remain unbroken (same API, new size defaults to md)
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate GlassSelect consumers, RoleSwitcher, and delete GlassSelect.tsx</name>
  <files>
    frontend-dashboard/src/components/settings/AuditLogSection.tsx,
    frontend-dashboard/src/components/settings/TeamDefaultsSection.tsx,
    frontend-dashboard/src/components/rbac/RoleSwitcher.tsx,
    frontend-dashboard/src/components/settings/GlassSelect.tsx
  </files>
  <action>
**AuditLogSection.tsx:**
- Change import: `import { GlassSelect } from './GlassSelect'` → `import { Select } from '@shared/components/ui/Select'`
- Replace every `<GlassSelect` with `<Select`
- The `ariaLabel` prop maps directly to the new `ariaLabel` prop on unified Select (same name, same behavior)
- Remove any `maxWidth` prop from the Select — if the GlassSelect had maxWidth, wrap the `<Select>` in a `<div style={{ maxWidth: '...' }}>` instead
- Add `size="md"` prop to each Select
- The `onChange` signature is already `(value: string) => void` on GlassSelect, so no handler changes needed

**TeamDefaultsSection.tsx:**
- Change import: `import { GlassSelect } from './GlassSelect'` → `import { Select } from '@shared/components/ui/Select'`
- Replace every `<GlassSelect` with `<Select`
- The `maxWidth="320px"` prop: remove from Select, wrap in `<div style={{ maxWidth: '320px' }}>` instead
- Map `ariaLabel` prop to the new `ariaLabel` prop
- Add `size="md"` prop

**RoleSwitcher.tsx:**
- Add import: `import { Select } from '@shared/components/ui/Select'`
- Replace the native `<select>` block with:
  ```tsx
  <Select
    options={allRoles.map((role) => ({
      value: role,
      label: ROLE_LABELS[role] ?? role,
    }))}
    value={activeRole}
    onChange={onRoleSwitch}
    size="sm"
    fullWidth={false}
    ariaLabel="Switch dashboard view role"
    autoFlip={true}
  />
  ```
- Update the container `styles.container` to remove the elevated surface pill styling:
  - Remove `background: 'var(--surface-elevated)'`
  - Remove `backdropFilter` and `WebkitBackdropFilter`
  - Remove `border: '1px solid var(--border-subtle)'`
  - Keep `display: 'flex'`, `alignItems: 'center'`, `gap: '8px'`
  - Keep or adjust padding to just `padding: '0'` (transparent, no chip)
- Remove the `styles.select` and `styles.option` entries (no longer used)
- Remove the `import React from 'react'` if only using JSX (React 18 automatic JSX transform)

**Delete GlassSelect.tsx:**
- Delete the file `frontend-dashboard/src/components/settings/GlassSelect.tsx` entirely

**Verification:** After changes, run `cd frontend-dashboard && npm run build:check` to confirm zero TypeScript errors. grep for any remaining `GlassSelect` imports — must find zero results.
  </action>
  <verify>
    <automated>cd frontend-dashboard && npm run build:check && grep -r "GlassSelect" frontend-dashboard/src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | wc -l</automated>
  </verify>
  <done>
- AuditLogSection.tsx imports Select from @shared, no GlassSelect reference
- TeamDefaultsSection.tsx imports Select from @shared, maxWidth handled via wrapper div
- RoleSwitcher.tsx uses unified Select with size="sm", fullWidth={false}, autoFlip={true}, transparent container
- GlassSelect.tsx file is deleted
- grep for "GlassSelect" across frontend-dashboard/src returns 0 results
- TypeScript build passes with zero errors
  </done>
</task>

</tasks>

<verification>
1. `cd frontend-dashboard && npm run build:check` — zero TypeScript errors
2. `grep -r "GlassSelect" frontend-dashboard/src/ --include="*.tsx" --include="*.ts"` — zero results
3. Confirm `shared/components/ui/Select.tsx` exports both `Select` and `SelectOption`
4. Confirm `data-lenis-prevent` attribute present on the dropdown panel div in Select.tsx
5. Confirm chevron SVG stroke is `%23c7b3ba` (muted), not `%23ffd54f` (gold)
6. Confirm no `boxShadow` on trigger when open
</verification>

<success_criteria>
- Unified Select component at shared/components/ui/Select.tsx with glass styling, 3 size presets, autoFlip, ariaLabel, keyboard nav, and ARIA attributes
- All GlassSelect consumers migrated to unified Select
- RoleSwitcher uses unified Select sm size with transparent container
- GlassSelect.tsx deleted with zero remaining references
- TypeScript build clean
</success_criteria>

<output>
After completion, create `.planning/phases/34.2-dropdown-menu-styling-across-all-roles-and-pages/34.2-01-SUMMARY.md`
</output>
