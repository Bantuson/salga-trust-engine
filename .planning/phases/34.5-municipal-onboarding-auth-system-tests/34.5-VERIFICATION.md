---
phase: 34.5-municipal-onboarding-auth-system-tests
verified: 2026-03-04T00:00:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Run auth-login.spec.ts against a live Supabase instance"
    expected: "All 7 tests pass — each role lands on correct dashboard heading, invalid credentials show error"
    why_human: "Tests require live Supabase auth + running frontend dev server at ports 5173/5174; cannot verify programmatically against a real auth backend"
  - test: "Run onboarding-pipeline.spec.ts against live app"
    expected: "3+3+5 tests pass — form validation catches empty/invalid fields, SALGA admin approve/reject updates UI, wizard step navigation advances and returns correctly"
    why_human: "Requires running frontend dev servers; department step disabled-button gate requires React state to render"
---

# Phase 34.5: Municipal Onboarding & Auth System Tests — Verification Report

**Phase Goal:** Replace the stale graceful-skip onboarding and auth E2E specs with strict, comprehensive tests. Delete onboarding.spec.ts and onboarding-journey.spec.ts. Rewrite OnboardingWizardPage page object for the 9-step v2 wizard. Create auth-login.spec.ts (5-role login + invalid credentials) and onboarding-pipeline.spec.ts (request-access form validation, SALGA admin approval/rejection, wizard step navigation). All tests use strict assertions — no graceful skip patterns.
**Verified:** 2026-03-04T00:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | OnboardingWizardPage page object matches the current 9-step v2 wizard (correct storage key, step labels, step indicator locators) | VERIFIED | `OnboardingWizardPage.ts` line 91 `localStorage.removeItem('salga_onboarding_wizard_v3')` matches frontend `STORAGE_KEY = 'salga_onboarding_wizard_v3'` (OnboardingWizardPage.tsx line 129). `STEPS` array (9 items) and `STEP_LABELS` map match frontend exactly. Locator `/Step \d+ of 9:/i` matches `<span>Step {currentIndex + 1} of {steps.length}: ...</span>` (frontend line 148-149). |
| 2 | Auth login tests for 5 key roles pass with strict assertions — no graceful skip patterns | VERIFIED | `auth-login.spec.ts` has 7 tests covering admin, salga_admin, cfo, municipal_manager, citizen. Only `test.skip` occurrence is in the file header comment (line 8), not an actual call. No `.catch(() => false)` chains anywhere in the spec. |
| 3 | Each role lands on correct role-specific dashboard after login | VERIFIED | Each of the 5 role tests uses `waitForURL` then `expect(page.locator('h1, h2').filter({ hasText: /.../ }).first()).toBeVisible({ timeout: 15000 })`. Headings: admin→`/Municipal Operations Dashboard/i`, salga_admin→`/SALGA Admin/i`, cfo→`/CFO Dashboard/i`, municipal_manager→`/Municipal Manager Dashboard/i`, citizen→URL contains `/profile`. |
| 4 | Invalid credentials test fails visibly — error state element appears in the UI | VERIFIED | `auth-login.spec.ts` lines 190-196: `const errorVisible = await page.locator('div, p, span').filter({ hasText: /invalid|failed|error|incorrect|wrong/i }).first().isVisible({ timeout: 10000 }); expect(errorVisible).toBe(true);` plus URL still contains `/login`. |
| 5 | Old onboarding.spec.ts and onboarding-journey.spec.ts are deleted | VERIFIED | `e2e-tests/tests/municipal/onboarding.spec.ts` — confirmed absent. `e2e-tests/tests/journeys/onboarding-journey.spec.ts` — confirmed absent. No references to either file anywhere in the codebase. |
| 6 | Request-access form validation tests catch missing required fields without graceful skip | VERIFIED | `onboarding-pipeline.spec.ts` Section 1 (3 tests) uses `base()` fixture (public, no auth). Empty submit triggers `expect(muniError).toBeVisible()` and `expect(demaError).toBeVisible()`. Invalid email asserts `expect(emailError).toBeVisible()`. Zero `.catch()` chains. |
| 7 | SALGA admin approval and rejection flow works in tests | VERIFIED | `onboarding-pipeline.spec.ts` Section 2 (3 tests): sees pending rows + "Approve & Onboard" button visible; clicking Approve navigates to `/onboarding`; Reject triggers compound assertion (count decrease OR rejected badge). All assertions are strict `expect()` calls. |
| 8 | Onboarding wizard 9-step navigation tested (step indicator, forward, back, department gate) | VERIFIED | `onboarding-pipeline.spec.ts` Section 3 (5 tests): step indicator matches `/Step \d+ of 9/i`; `getCurrentStep()` returns `'welcome'`; advance button regex `/Confirm.*Continue|Start Setup|Get Started|Begin|^Next$/i` advances to step 2; Back returns to step 1; department `Next` button `toBeDisabled()` when no departments added. |

**Score:** 8/8 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `e2e-tests/fixtures/page-objects/dashboard/OnboardingWizardPage.ts` | Rewritten page object for 9-step v2 wizard with correct storage key and step indicator | VERIFIED | 200 lines. `STEPS` 9-element `as const` tuple, `STEP_LABELS` matching frontend, `stepIndicator` locator `span` filtered by `/Step \d+ of 9:/i`, storage key `salga_onboarding_wizard_v3`, `getCurrentStep()` and `getStepNumber()` parse step indicator text. |
| `e2e-tests/tests/municipal/auth-login.spec.ts` | Strict auth login tests for 5 roles + invalid credentials + GSAP suppression | VERIFIED | 228 lines, 7 tests, `suppressGsap()` helper, `test.slow()` at describe level, `finally { await context.close() }` cleanup. |
| `e2e-tests/tests/municipal/onboarding-pipeline.spec.ts` | Strict E2E tests for full SALGA-driven onboarding pipeline | VERIFIED | 333 lines, 11 tests across 3 describe blocks. `OnboardingWizardPage` imported and used in Section 3. |
| `e2e-tests/tests/municipal/onboarding.spec.ts` | DELETED (was stale) | VERIFIED (absent) | File confirmed absent from `e2e-tests/tests/municipal/`. |
| `e2e-tests/tests/journeys/onboarding-journey.spec.ts` | DELETED (was stale) | VERIFIED (absent) | File confirmed absent from `e2e-tests/tests/journeys/`. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `auth-login.spec.ts` | `fixtures/auth.ts` | `import { test, expect } from '../../fixtures/auth'` | WIRED | Line 17. `test` exports `browser` fixture used in all 7 tests. |
| `auth-login.spec.ts` | `LoginPage.ts` | Direct locators (no LoginPage import) | PARTIAL — ACCEPTABLE | Plan specified LoginPage import; implementation uses equivalent inline locators (`input[id="email"]`, `input[id="password"]`, `button[type="submit"]`). Functional contract identical to LoginPage. Not a blocker. |
| `onboarding-pipeline.spec.ts` | `fixtures/auth.ts` | `import { test as base, test, expect } from '../../fixtures/auth'` | WIRED | Line 19. `base` used for unauthenticated Section 1, `test` used for authenticated Sections 2-3. `salgaAdminPage` and `adminPage` fixtures confirmed in auth.ts. |
| `onboarding-pipeline.spec.ts` | `OnboardingWizardPage.ts` | `import { OnboardingWizardPage } from '../../fixtures/page-objects/dashboard/OnboardingWizardPage'` | WIRED | Line 20. `OnboardingWizardPage` instantiated in all 5 Section 3 tests. |
| `OnboardingWizardPage.ts` | `frontend-dashboard/src/pages/OnboardingWizardPage.tsx` | Storage key `salga_onboarding_wizard_v3` + step locators match frontend | WIRED | Storage key matches line 129 of frontend. Step indicator `<span>Step N of {steps.length}: label</span>` (frontend line 148-149) matches locator `/Step \d+ of 9:/i`. 9 STEPS and STEP_LABELS match exactly. |
| `onboarding-pipeline.spec.ts` | `RequestAccessPage.tsx` | Form field locators match label text | WIRED | Assertions use `/municipality name.*required/i` and `/demarcation code.*required/i` matching `RequestAccessPage.tsx` validation messages. |
| `onboarding-pipeline.spec.ts` | `AccessRequestsPage.tsx` | `Approve.*Onboard` button + `/onboarding` navigation | WIRED | `handleApproveAndOnboard` calls `navigate('/onboarding', {...})`. Test asserts `waitForURL(/\/onboarding/)`. |

---

### Requirements Coverage

No requirement IDs declared in either plan's `requirements` field (`requirements: []`). This is explicit urgent work outside the numbered requirements system. No REQUIREMENTS.md coverage needed.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `OnboardingWizardPage.ts` | 111 | `.catch(() => false)` in `goto()` return | INFO | Page object's `goto()` returns `boolean` but the pipeline spec never checks the return value — it proceeds to strict `expect(wizard.stepIndicator).toBeVisible({ timeout: 15000 })`. The `.catch()` softens the `goto()` return but does NOT suppress spec-level assertion failures. The test will still fail if the step indicator is not visible. |
| `OnboardingWizardPage.ts` | 119-134 | `try/catch` in `getCurrentStep()` returning `'unknown'` | INFO | Plan stated "page object should throw on timeout — spec decides." The `getCurrentStep()` method swallows errors and returns `'unknown'`. The spec asserts `expect(step).toBe('welcome')` which would fail on `'unknown'`, so regression catching is preserved. |

No STUB, MISSING, or BLOCKER patterns found in the spec files.

---

### Human Verification Required

#### 1. Auth Login Flow — Live Supabase Validation

**Test:** Start `cd frontend-dashboard && npm run dev` (port 5173) and run `cd e2e-tests && npx playwright test tests/municipal/auth-login.spec.ts --project=dashboard-chromium`
**Expected:** All 7 tests pass — admin/salga_admin/cfo/municipal_manager land on correct dashboard headings; citizen lands on /profile; invalid credentials show error; empty form stays on /login
**Why human:** Requires live Supabase auth backend + running frontend. Test credentials (`admin@test-jozi-001.test`, etc.) must exist in the test Supabase project with correct roles.

#### 2. Onboarding Pipeline — End-to-End Flow

**Test:** Start both frontend servers and run `cd e2e-tests && npx playwright test tests/municipal/onboarding-pipeline.spec.ts --project=dashboard-chromium`
**Expected:** 11 tests pass — form validation shows errors on empty/invalid submit; SALGA admin sees mock pending requests and can approve (navigates to /onboarding) or reject; wizard shows "Step 1 of 9: Welcome", advances/returns, and department Next is disabled when empty.
**Why human:** Requires running frontend dev server. Department step gate (`toBeDisabled()`) depends on React state. Access requests page uses mock data (Drakenstein ar-1, Emfuleni ar-2) which must render correctly.

---

### Gaps Summary

No gaps identified. All must-haves from both plans are verified:

- `OnboardingWizardPage.ts` is fully rewritten with the correct `salga_onboarding_wizard_v3` storage key, 9-step STEPS/STEP_LABELS matching the frontend, step indicator locator pattern matching the `<span>` in compact mode, and `getStepNumber()`/`getCurrentStep()` methods.
- `auth-login.spec.ts` has 7 strict tests covering all 5 required roles plus 2 negative cases. No `test.skip()` calls (only in a comment). Each test uses fresh browser context with `finally { context.close() }` cleanup.
- `onboarding-pipeline.spec.ts` has 11 strict tests in 3 describe blocks. Zero `test.skip()` calls. Zero `.catch(() => false)` assertion chains.
- Both stale specs (`onboarding.spec.ts`, `onboarding-journey.spec.ts`) are deleted with no remaining references.
- All 3 commits documented in SUMMARY files are verified in git history (4e2f058, 397efb6, 07d873c).
- TypeScript compilation errors are pre-existing in `global-setup.ts` (unrelated to this phase) and were present before Phase 34.5 began.

---

_Verified: 2026-03-04T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
