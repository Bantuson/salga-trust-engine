---
phase: 33-comprehensive-gap-closure
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - .planning/phases/27-rbac-foundation-tenant-configuration/27-VERIFICATION.md
autonomous: true
requirements:
  - RBAC-03
  - RBAC-05
  - RBAC-06

must_haves:
  truths:
    - "Phase 27 has an independent VERIFICATION.md documenting test results for RBAC-01 through RBAC-06"
    - "RBAC-03, RBAC-05, RBAC-06 are marked VERIFIED in the document with test evidence"
    - "RBAC-01 and RBAC-02 show their actual verification status (partial — route fixes were in Phase 33 Plan 01)"
    - "Phase 27 test suite passes: pytest tests/test_rbac_phase27.py tests/test_departments_api.py tests/test_pms_readiness.py"
  artifacts:
    - path: ".planning/phases/27-rbac-foundation-tenant-configuration/27-VERIFICATION.md"
      provides: "Independent verification report for Phase 27"
      contains: "RBAC-01"
  key_links:
    - from: "27-VERIFICATION.md requirements table"
      to: "tests/test_rbac_phase27.py test output"
      via: "pytest run documented in verification"
      pattern: "RBAC-0[123456]"
---

<objective>
Write the independent Phase 27 VERIFICATION.md that documents actual test results for RBAC-01 through RBAC-06, closing the verification gap identified in the v2.0 milestone audit.

Purpose: Phase 27 has only a VALIDATION.md (self-assessment); it lacks an independent post-execution verification document. This plan runs the Phase 27 test suite, captures results, and produces 27-VERIFICATION.md in the same format as 29-VERIFICATION.md and 30-VERIFICATION.md.
Output: `.planning/phases/27-rbac-foundation-tenant-configuration/27-VERIFICATION.md`
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-rbac-foundation-tenant-configuration/27-VALIDATION.md
@.planning/phases/29-individual-performance-agreements/29-VERIFICATION.md

## Verification format reference

29-VERIFICATION.md is the canonical format. 27-VERIFICATION.md must follow the same structure:
1. YAML frontmatter (phase, verified, status, score, gaps if any)
2. Phase Goal statement
3. Observable Truths table (# | Truth | Status | Evidence)
4. Required Artifacts table (Artifact | Expected | Status | Details)
5. Key Link Verification table (From | To | Via | Status | Details)
6. Requirements Coverage table (Req ID | Source Plan | Description | Status | Evidence)
7. Anti-Patterns Found (or "None found")
8. Human Verification Required (if applicable)
9. Gaps Summary (or "No gaps")

## Phase 27 test files (all exist per research):
- tests/test_rbac_phase27.py — covers RBAC-01 (roles), RBAC-04 (hierarchy), RBAC-06 (audit)
- tests/test_departments_api.py — covers RBAC-02 (department CRUD), RBAC-05 (organogram)
- tests/test_pms_readiness.py — covers RBAC-04 (PMS gate), RBAC-01 (role prerequisites)
- tests/test_rbac_coverage.py — supplemental coverage

## Phase 27 success criteria (from ROADMAP.md):
1. Admin can assign any of the 14 new roles and user's JWT reflects new role
2. Admin can create, name, and assign director to departments; organogram reflects hierarchy
3. Senior role inherits access of subordinate roles (require_min_tier)
4. Every role change visible in audit log
5. PMS feature endpoints return 403 until department configuration complete
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 27 test suite and write 27-VERIFICATION.md</name>
  <files>
    .planning/phases/27-rbac-foundation-tenant-configuration/27-VERIFICATION.md
  </files>
  <action>
    **Step 1 — Run the Phase 27 test suite and capture results.**

    Run each test file and note pass/fail counts:
    ```bash
    cd /c/Users/Bantu/mzansi-agentive/salga-trust-engine
    pytest tests/test_rbac_phase27.py tests/test_departments_api.py tests/test_pms_readiness.py tests/test_rbac_coverage.py -v --tb=short 2>&1 | tail -60
    ```

    Note: Tests run in SQLite in-memory mode (no PostgreSQL needed). Record:
    - Total tests found
    - Tests passed
    - Tests failed (if any, note which and why)
    - Any ERROR-level failures distinct from test failures

    **Step 2 — Inspect key source files to verify artifacts exist.**

    Check these files exist and have the required content:
    - `src/models/user.py` — UserRole enum has all 18 roles, TIER_ORDER dict
    - `src/models/department.py` — Department model with TenantAwareModel inheritance
    - `src/api/v1/roles.py` — role assignment endpoints, tier1 approval API
    - `src/api/v1/departments.py` — department CRUD + organogram endpoint
    - `src/services/pms_readiness.py` — require_pms_ready() factory
    - `alembic/versions/` — Phase 27 migrations exist

    For each file, use a quick grep to confirm key exports:
    ```bash
    grep -c "UserRole\|TIER_ORDER\|EXECUTIVE_MAYOR\|SALGA_ADMIN" src/models/user.py
    grep -c "class Department\|TenantAwareModel" src/models/department.py
    grep -c "require_pms_ready\|PMS_NOT_READY" src/services/pms_readiness.py
    grep -c "organogram\|/departments" src/api/v1/departments.py
    ```

    **Step 3 — Write 27-VERIFICATION.md.**

    Create `.planning/phases/27-rbac-foundation-tenant-configuration/27-VERIFICATION.md` following EXACTLY the format of `29-VERIFICATION.md` (read it for reference).

    The document must include:

    **Frontmatter:**
    ```yaml
    ---
    phase: 27-rbac-foundation-tenant-configuration
    verified: 2026-03-02T00:00:00Z  # use today's date
    status: verified  # or gaps_found if tests fail
    score: X/6 must-haves verified
    gaps: []  # populate if any truths fail
    human_verification: []  # populate if any human checks needed
    ---
    ```

    **Observable Truths table** — verify each against test results and code inspection:
    | # | Truth | Status | Evidence |
    1. Admin can assign any of 18 roles via PUT /api/v1/roles/{user_id}/assign; JWT reflects new role (RBAC-01) — VERIFIED if test_rbac_phase27.py tests pass
    2. Admin can create departments, name them, assign director (RBAC-02) — VERIFIED if test_departments_api.py passes; note that /departments frontend route was missing (fixed in Plan 33-01, not a backend gap)
    3. Municipality settings configurable (category, province, demarcation, SDBIP layers, scoring) (RBAC-03) — VERIFIED if settings endpoints tested
    4. Senior roles inherit subordinate access via require_min_tier() (RBAC-04) — VERIFIED if TIER_ORDER and require_min_tier tests pass
    5. Organogram endpoint returns tree structure (RBAC-05) — VERIFIED if GET /departments/organogram returns nested departments
    6. Role changes produce ROLE_CHANGE audit log entries (RBAC-06) — VERIFIED if audit event tests pass

    **Required Artifacts table** — for each file:
    - `src/models/user.py` — UserRole enum (18 roles), TIER_ORDER dict, TierApprovalRequest model
    - `src/models/department.py` — Department, DepartmentTicketCategoryMap models
    - `src/models/municipality.py` — Municipality with PMS settings fields
    - `src/api/v1/roles.py` — role assignment, tier 1 approval endpoints
    - `src/api/v1/departments.py` — department CRUD, organogram endpoint
    - `src/services/pms_readiness.py` — require_pms_ready() factory
    - `alembic/versions/` — Phase 27 migrations
    - `tests/test_rbac_phase27.py` — test suite (note count)
    - `tests/test_departments_api.py` — test suite (note count)
    - `tests/test_pms_readiness.py` — test suite (note count)

    **Key Links table** — verify connections:
    - `src/api/v1/roles.py` → `src/models/user.py` via UserRole enum import
    - `src/api/deps.py` → TIER_ORDER dict via require_min_tier()
    - `src/api/v1/departments.py` → organogram builder (Python, not SQL)
    - `src/services/pms_readiness.py` → Municipality.is_pms_configured property

    **Requirements Coverage table:**
    | RBAC-01 | Phase 27 | ... | VERIFIED or PARTIAL |
    | RBAC-02 | Phase 27 | ... | VERIFIED (backend); /departments route fixed in Phase 33 |
    | RBAC-03 | Phase 27 | ... | VERIFIED |
    | RBAC-04 | Phase 27 | ... | VERIFIED |
    | RBAC-05 | Phase 27 | ... | VERIFIED |
    | RBAC-06 | Phase 27 | ... | VERIFIED |

    **Anti-Patterns Found:** Document any if found, or write "No anti-patterns found in Phase 27 backend implementation."

    **Gaps Summary:** If all tests pass and artifacts exist, write:
    "Phase 27 backend implementation is fully verified. The only gap was frontend routing (/departments had no route) which was fixed in Phase 33 Plan 33-01. RBAC-01 through RBAC-06 backend requirements are all satisfied."

    If tests fail or artifacts are missing, document the specific gap with same format as 29-VERIFICATION.md gaps section.

    Be precise about test counts — include the actual numbers from the pytest run, not estimates.
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/salga-trust-engine && pytest tests/test_rbac_phase27.py tests/test_departments_api.py tests/test_pms_readiness.py -v --tb=short -q 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `.planning/phases/27-rbac-foundation-tenant-configuration/27-VERIFICATION.md` exists
    - Document has YAML frontmatter, all 5 required sections (Truths, Artifacts, Key Links, Requirements Coverage, Anti-Patterns)
    - RBAC-03, RBAC-05, RBAC-06 marked VERIFIED with specific test evidence
    - RBAC-01, RBAC-02, RBAC-04 show correct status (VERIFIED for backend, noting frontend fix in Plan 33-01 for RBAC-01/02)
    - Test counts from actual pytest run (not estimates) documented
    - Format matches 29-VERIFICATION.md structure
  </done>
</task>

</tasks>

<verification>
File exists: `.planning/phases/27-rbac-foundation-tenant-configuration/27-VERIFICATION.md`
Contains all required sections: `grep -c "Observable Truths\|Required Artifacts\|Key Link\|Requirements Coverage" .planning/phases/27-rbac-foundation-tenant-configuration/27-VERIFICATION.md`
Phase 27 tests still pass: `pytest tests/test_rbac_phase27.py tests/test_departments_api.py tests/test_pms_readiness.py -q`
</verification>

<success_criteria>
1. `27-VERIFICATION.md` exists at the correct path
2. Document follows the 29-VERIFICATION.md format (5 main sections)
3. Requirements RBAC-03, RBAC-05, RBAC-06 are documented as VERIFIED
4. Actual pytest test counts are recorded (not placeholder numbers)
5. Any real gaps found during verification are documented with specifics
</success_criteria>

<output>
After completion, create `.planning/phases/33-comprehensive-gap-closure/33-02-SUMMARY.md`
</output>
