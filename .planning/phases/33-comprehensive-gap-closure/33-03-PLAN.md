---
phase: 33-comprehensive-gap-closure
plan: 03
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - .planning/phases/28-idp-sdbip-core-performance-monitoring/28-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - RBAC-01
  - RBAC-02
  - RBAC-04
  - REPORT-03
  - REPORT-04
  - REPORT-09

must_haves:
  truths:
    - "Phase 28 has an independent VERIFICATION.md documenting test results for IDP-01 through IDP-05, SDBIP-01 through SDBIP-10, EVID-01 through EVID-08"
    - "All 23 Phase 28 requirements are marked VERIFIED or PARTIAL with specific evidence"
    - "IDP-04 is documented as VERIFIED (backend golden thread API exists and returns data)"
    - "REQUIREMENTS.md checkboxes match actual verification status: RBAC-01, RBAC-02, RBAC-04 show [ ] pending (Phase 33 fixes them but REQUIREMENTS.md only updates after full phase completion)"
    - "REPORT-03, REPORT-04, REPORT-09 remain [x] in REQUIREMENTS.md (they were already correct — do not regress)"
    - "Phase 28 test suite passes: pytest tests/test_pms_idp.py tests/test_pms_sdbip.py tests/test_pms_actuals.py tests/test_pms_auto_populate.py tests/test_pms_evidence.py"
  artifacts:
    - path: ".planning/phases/28-idp-sdbip-core-performance-monitoring/28-VERIFICATION.md"
      provides: "Independent verification report for Phase 28"
      contains: "IDP-04"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate tracking — RBAC-01/02/04 to be updated to [x] after Phase 33 plans complete"
      contains: "RBAC-01"
  key_links:
    - from: "28-VERIFICATION.md requirements table"
      to: "tests/test_pms_idp.py test output"
      via: "pytest run documented in verification"
      pattern: "IDP-0[12345]|SDBIP-0[0-9]|EVID-0[0-9]"
    - from: "REQUIREMENTS.md RBAC-01 checkbox"
      to: "Phase 33 completion"
      via: "tracking update at phase close"
      pattern: "RBAC-01.*Phase 33"
---

<objective>
Write the independent Phase 28 VERIFICATION.md and update REQUIREMENTS.md to accurately reflect the current status of all requirements — completing the v2.0 gap closure tracking work.

Purpose: Phase 28 has only a VALIDATION.md (self-assessment); it lacks an independent post-execution verification document. Additionally, REQUIREMENTS.md needs its Phase 33 requirements (RBAC-01, RBAC-02, RBAC-04) updated to [x] once their fixes are confirmed complete (which Plan 33-01 implements).
Output: `.planning/phases/28-idp-sdbip-core-performance-monitoring/28-VERIFICATION.md` + updated REQUIREMENTS.md.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-idp-sdbip-core-performance-monitoring/28-VALIDATION.md
@.planning/phases/29-individual-performance-agreements/29-VERIFICATION.md
@.planning/REQUIREMENTS.md

## Phase 28 test files (all exist per research):
- tests/test_pms_idp.py — IDP-01, IDP-02, IDP-03, IDP-04, IDP-05
- tests/test_pms_sdbip.py — SDBIP-01 through SDBIP-10
- tests/test_pms_actuals.py — EVID-01, EVID-02, EVID-04, EVID-05, EVID-06, EVID-08
- tests/test_pms_auto_populate.py — SDBIP-07, SDBIP-08, EVID-08
- tests/test_pms_evidence.py — EVID-03, EVID-07

## Phase 28 success criteria (from ROADMAP.md):
1. PMS officer can create 5-year IDP cycle, add goals/objectives with KPA alignment, view IDP-to-SDBIP golden thread
2. Director can create departmental SDBIP KPIs with quarterly targets, link to mSCOA code, submit for Mayor sign-off
3. Director can submit quarterly actual values, upload evidence, see traffic-light status automatically
4. After each quarter, Celery auto-population fills SDBIP actuals from tickets (never GBV)
5. Once validated, actuals are immutable — corrections require new record with full audit trail

## REQUIREMENTS.md current state (from direct inspection):
- RBAC-01: [ ] (pending — Plan 33-01 fixes the frontend route and PMS gate; mark [x] when Phase 33 complete)
- RBAC-02: [ ] (pending — Plan 33-01 creates DepartmentsPage; mark [x] when Phase 33 complete)
- RBAC-04: [ ] (pending — Plan 33-01 adds require_pms_ready gate; mark [x] when Phase 33 complete)
- IDP-04: [ ] (pending — Plan 33-01 adds standalone route; mark [x] when Phase 33 complete)
- PA-01: [ ] (pending — already fixed in current code; mark [x])
- REPORT-05: [ ] (pending — Plan 33-01 fixes MUNICIPAL_MANAGER gate; mark [x] when Phase 33 complete)
- REPORT-03: [x] (already correct — do NOT change)
- REPORT-04: [x] (already correct — do NOT change)
- REPORT-09: [x] (already correct — do NOT change)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 28 test suite and write 28-VERIFICATION.md</name>
  <files>
    .planning/phases/28-idp-sdbip-core-performance-monitoring/28-VERIFICATION.md
  </files>
  <action>
    **Step 1 — Run the Phase 28 test suite and capture results.**

    Run each test file and record precise pass/fail counts:
    ```bash
    cd /c/Users/Bantu/mzansi-agentive/salga-trust-engine
    pytest tests/test_pms_idp.py tests/test_pms_sdbip.py tests/test_pms_actuals.py tests/test_pms_auto_populate.py tests/test_pms_evidence.py -v --tb=short 2>&1 | tail -80
    ```

    Record:
    - Total tests per file
    - Total passed, failed, errored
    - Any specific failures with test name and brief error message

    **Step 2 — Verify key artifact existence and content.**

    Quick checks (use grep -c for counts, not full file reads):
    ```bash
    grep -c "class IDPCycle\|class IDPGoal\|class IDPObjective\|class IDPVersion" src/models/idp.py
    grep -c "class SDBIPScorecard\|class SDBIPKpi\|class SDBIPQuarterlyTarget" src/models/sdbip.py
    grep -c "class SDBIPActual\|is_validated\|corrects_actual_id" src/models/sdbip.py
    grep -c "class EvidenceDocument\|virus_scan_status" src/models/evidence.py
    grep -c "class MscoaReference\|NonTenantModel" src/models/sdbip.py
    grep -c "compute_achievement\|traffic_light\|green\|amber\|red" src/services/sdbip_service.py
    grep -c "class AutoPopulationEngine\|is_sensitive.*False\|SEC.05" src/services/auto_population.py
    grep -c "golden.thread\|golden_thread\|GoldenThread" src/api/v1/idp.py
    grep -c "GoldenThreadPage\|golden.thread" frontend-dashboard/src/pages/GoldenThreadPage.tsx
    ```

    **Step 3 — Write 28-VERIFICATION.md.**

    Create `.planning/phases/28-idp-sdbip-core-performance-monitoring/28-VERIFICATION.md` following EXACTLY the format of `29-VERIFICATION.md`.

    **Frontmatter:**
    ```yaml
    ---
    phase: 28-idp-sdbip-core-performance-monitoring
    verified: 2026-03-02T00:00:00Z
    status: verified  # or gaps_found if tests fail
    score: X/5 must-haves verified  # based on the 5 ROADMAP success criteria
    gaps: []
    human_verification: []
    ---
    ```

    **Observable Truths (5 from ROADMAP success criteria):**
    1. PMS officer can create IDP cycle, add goals/objectives with KPA alignment, view golden thread (IDP-01/02/03/04/05)
    2. Director can create SDBIP KPIs with quarterly targets, link to mSCOA code, submit for Mayor sign-off (SDBIP-01 through SDBIP-06)
    3. Director can submit quarterly actuals, upload evidence, see traffic-light status (EVID-01 through EVID-06)
    4. Celery auto-population fills actuals from tickets (never GBV — is_sensitive=False filter) (SDBIP-07/08, EVID-08)
    5. Validated actuals are immutable; corrections require new record (EVID-04/05)

    **Required Artifacts table** — for each file:
    - `src/models/idp.py` — IDPCycle, IDPGoal, IDPObjective, IDPVersion, IDPWorkflow
    - `src/models/sdbip.py` — SDBIPScorecard, SDBIPKpi, SDBIPQuarterlyTarget, SDBIPActual, MscoaReference, SDBIPTicketAggregationRule
    - `src/models/evidence.py` — EvidenceDocument with virus_scan_status
    - `src/schemas/idp.py` — IDP Pydantic schemas
    - `src/schemas/sdbip.py` — SDBIP Pydantic schemas
    - `src/services/idp_service.py` — IDPService with golden thread method
    - `src/services/sdbip_service.py` — SDBIPService with compute_achievement and traffic_light
    - `src/services/auto_population.py` — AutoPopulationEngine with SEC-05 filter
    - `src/api/v1/idp.py` — IDP CRUD + golden thread endpoint
    - `src/api/v1/sdbip.py` — SDBIP CRUD + actuals + mSCOA endpoints
    - `src/api/v1/evidence.py` — evidence upload/download/validation
    - `src/tasks/auto_populate_task.py` — Celery beat task
    - Alembic migrations for Phase 28 tables
    - `frontend-dashboard/src/pages/GoldenThreadPage.tsx` — golden thread UI
    - `tests/test_pms_idp.py` — IDP test suite (record count)
    - `tests/test_pms_sdbip.py` — SDBIP test suite (record count)
    - `tests/test_pms_actuals.py` — actuals test suite (record count)
    - `tests/test_pms_auto_populate.py` — auto-population test suite (record count)
    - `tests/test_pms_evidence.py` — evidence test suite (record count)

    **Key Links table** — verify connections:
    - `src/api/v1/idp.py` → golden thread endpoint → `src/services/idp_service.py`
    - `src/services/sdbip_service.py` → `compute_achievement` → returns (Decimal, str) traffic_light
    - `src/services/auto_population.py` → SEC-05 filter → `AND is_sensitive = FALSE`
    - `src/tasks/auto_populate_task.py` → Celery beat schedule → registered in celery_app.py
    - `SDBIPActual.corrects_actual_id` FK → correction chain audit trail

    **Requirements Coverage table:** Cover all 23 requirements (IDP-01 through IDP-05, SDBIP-01 through SDBIP-10, EVID-01 through EVID-08).

    For IDP-04 specifically: document that the BACKEND golden thread endpoint exists and is tested (VERIFIED), and that the FRONTEND standalone route was missing but added in Phase 33 Plan 33-01.

    **Key decisions to note (from STATE.md):**
    - SDBIPKpi forward relationship not declared on IDPObjective in Wave 1 (Phase 28-04 adds it)
    - Division by zero (target=0) returns (Decimal('0'), 'red') — graceful degradation
    - Correction chain uses corrects_actual_id FK (not soft-delete)
    - SEC-05 filter: `is_sensitive==False` at query level, cannot be disabled per-rule
    - Raw SQL text() for cross-tenant discovery bypasses ORM event listener

    **Anti-Patterns Found:** Document any if found during inspection. Common ones to check:
    - Any endpoint that returns GBV ticket data without is_sensitive filter (would be SEC-05 violation)
    - Any hardcoded tenant_id in queries (would break multi-tenancy)
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/salga-trust-engine && pytest tests/test_pms_idp.py tests/test_pms_sdbip.py tests/test_pms_actuals.py tests/test_pms_auto_populate.py tests/test_pms_evidence.py -q --tb=short 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `.planning/phases/28-idp-sdbip-core-performance-monitoring/28-VERIFICATION.md` exists
    - All 5 required sections present (Truths, Artifacts, Key Links, Requirements Coverage, Anti-Patterns)
    - All 23 requirements (IDP-01 through IDP-05, SDBIP-01 through SDBIP-10, EVID-01 through EVID-08) documented with status
    - IDP-04 documented as backend VERIFIED / frontend route fixed in Phase 33
    - Actual pytest test counts recorded (not estimates)
    - Format matches 29-VERIFICATION.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to mark Phase 33 requirements as complete</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
    **Important prerequisite check:** This task must only run AFTER Plan 33-01 has been verified complete (the checkpoint passed). Since Plan 33-03 runs in Wave 2 and depends on 33-01, this condition is satisfied by the dependency graph.

    **Step 1 — Verify current state before editing.**

    Read REQUIREMENTS.md and confirm:
    - RBAC-01: currently `[ ]` → should become `[x]` (Plan 33-01 fixed frontend route + PMS gate)
    - RBAC-02: currently `[ ]` → should become `[x]` (Plan 33-01 created DepartmentsPage)
    - RBAC-04: currently `[ ]` → should become `[x]` (Plan 33-01 added require_pms_ready to role dashboards)
    - IDP-04: currently `[ ]` → should become `[x]` (Plan 33-01 registered /pms/golden-thread route)
    - PA-01: currently `[ ]` → should become `[x]` (already fixed in current PerformanceAgreementsPage.tsx)
    - REPORT-05: currently `[ ]` → should become `[x]` (Plan 33-01 added MUNICIPAL_MANAGER to submit_for_review)
    - REPORT-03: currently `[x]` → must STAY `[x]` (do not regress)
    - REPORT-04: currently `[x]` → must STAY `[x]` (do not regress)
    - REPORT-09: currently `[x]` → must STAY `[x]` (do not regress)

    **Step 2 — Update the checkbox lines.**

    In REQUIREMENTS.md, change these 6 lines from `[ ]` to `[x]`:

    ```
    - [ ] **RBAC-01**: Platform supports 4-tier role hierarchy...
    → - [x] **RBAC-01**: Platform supports 4-tier role hierarchy...

    - [ ] **RBAC-02**: Admin can configure municipal department structure...
    → - [x] **RBAC-02**: Admin can configure municipal department structure...

    - [ ] **RBAC-04**: Platform enforces role hierarchy inheritance...
    → - [x] **RBAC-04**: Platform enforces role hierarchy inheritance...

    - [ ] **IDP-04**: User can view alignment mapping from IDP objectives...
    → - [x] **IDP-04**: User can view alignment mapping from IDP objectives...

    - [ ] **PA-01**: PMS officer can create performance agreement...
    → - [x] **PA-01**: PMS officer can create performance agreement...

    - [ ] **REPORT-05**: Reports follow approval workflow...
    → - [x] **REPORT-05**: Reports follow approval workflow...
    ```

    **Step 3 — Update the Traceability section.**

    In the Traceability table at the bottom of REQUIREMENTS.md, update these rows:
    ```
    | RBAC-01 | Phase 33 | Pending |
    → | RBAC-01 | Phase 33 | Complete |

    | RBAC-02 | Phase 33 | Pending |
    → | RBAC-02 | Phase 33 | Complete |

    | RBAC-04 | Phase 33 | Pending |
    → | RBAC-04 | Phase 33 | Complete |

    | IDP-04 | Phase 33 | Pending |
    → | IDP-04 | Phase 33 | Complete |

    | PA-01 | Phase 33 | Pending |
    → | PA-01 | Phase 33 | Complete |

    | REPORT-05 | Phase 33 | Pending |
    → | REPORT-05 | Phase 33 | Complete |
    ```

    **Step 4 — Update the last-updated date.**

    At the bottom of REQUIREMENTS.md, update:
    ```
    *Last updated: 2026-02-28 ...
    → *Last updated: 2026-03-02 — Phase 33 gap closure: RBAC-01, RBAC-02, RBAC-04, IDP-04, PA-01, REPORT-05 marked complete*
    ```

    **Step 5 — Verify no regressions.**

    After editing, run:
    ```bash
    grep -c "\[ \]" .planning/REQUIREMENTS.md
    grep -c "\[x\]" .planning/REQUIREMENTS.md
    ```
    Expected: 0 unchecked `[ ]` items (all 60 requirements should be `[x]`).
    If any `[ ]` remain, they must be intentional deferred items in the v2.1+ section — verify those are NOT in the v2.0 requirements section.
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/salga-trust-engine && grep -c "\[ \]" .planning/REQUIREMENTS.md 2>&amp;1</automated>
  </verify>
  <done>
    - All 6 Phase 33 requirements (RBAC-01, RBAC-02, RBAC-04, IDP-04, PA-01, REPORT-05) show `[x]` in REQUIREMENTS.md
    - REPORT-03, REPORT-04, REPORT-09 remain `[x]` (not regressed)
    - Traceability table shows "Complete" for all 6
    - Last-updated timestamp reflects 2026-03-02
    - `grep -c "\[ \]" .planning/REQUIREMENTS.md` returns 0 (all v2.0 requirements checked)
  </done>
</task>

</tasks>

<verification>
Phase 28 verification file exists: `ls .planning/phases/28-idp-sdbip-core-performance-monitoring/28-VERIFICATION.md`
REQUIREMENTS.md has no unchecked v2.0 items: `grep -c "\[ \]" .planning/REQUIREMENTS.md` returns 0
Phase 28 tests still pass: `pytest tests/test_pms_idp.py tests/test_pms_sdbip.py -q --tb=short`
Traceability table updated: `grep "RBAC-01\|IDP-04\|PA-01\|REPORT-05" .planning/REQUIREMENTS.md` shows "Complete"
</verification>

<success_criteria>
1. `28-VERIFICATION.md` exists at the correct path with all 23 requirements documented
2. IDP-04 is marked VERIFIED (backend) with note about frontend route in Phase 33
3. `REQUIREMENTS.md` has 0 unchecked `[ ]` items in the v2.0 requirements section
4. Phase 28 test suite passes without regressions
5. Traceability table shows "Complete" for RBAC-01, RBAC-02, RBAC-04, IDP-04, PA-01, REPORT-05
</success_criteria>

<output>
After completion, create `.planning/phases/33-comprehensive-gap-closure/33-03-SUMMARY.md`
</output>
