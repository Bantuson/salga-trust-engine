---
phase: 28-idp-sdbip-core-performance-monitoring
plan: 07
type: execute
wave: 5
depends_on: ["28-01", "28-02", "28-03", "28-04", "28-05", "28-06"]
files_modified:
  - src/services/idp_service.py
  - src/api/v1/idp.py
  - frontend-dashboard/src/pages/IdpPage.tsx
  - frontend-dashboard/src/pages/IdpDetailPage.tsx
  - frontend-dashboard/src/pages/SdbipPage.tsx
  - frontend-dashboard/src/pages/SdbipKpiPage.tsx
  - frontend-dashboard/src/pages/ActualsPage.tsx
  - frontend-dashboard/src/pages/EvidencePage.tsx
  - frontend-dashboard/src/pages/GoldenThreadPage.tsx
  - frontend-dashboard/src/components/pms/TrafficLightBadge.tsx
  - frontend-dashboard/src/hooks/useRoleBasedNav.ts
  - frontend-dashboard/src/components/layout/Sidebar.tsx
  - frontend-dashboard/src/App.tsx
autonomous: false
requirements:
  - IDP-04
  - SDBIP-03
  - SDBIP-04

must_haves:
  truths:
    - "User can view the golden thread alignment: IDP Cycle -> Goals -> Objectives -> SDBIP KPIs in a tree view"
    - "PMS officer can navigate to IDP management page, create cycles, add goals and objectives"
    - "Director can view SDBIP scorecards, create KPIs with quarterly targets, and see achievement status"
    - "Director can submit quarterly actuals and see traffic-light status (green/amber/red)"
    - "User can upload evidence documents per quarterly actual"
    - "Sidebar navigation includes PMS section with IDP, SDBIP, Actuals, Evidence links for authorized roles"
  artifacts:
    - path: "src/services/idp_service.py"
      provides: "get_golden_thread method returning IDP -> Goals -> Objectives -> KPIs tree"
      contains: "get_golden_thread"
    - path: "src/api/v1/idp.py"
      provides: "GET /cycles/{cycle_id}/golden-thread endpoint"
      contains: "golden.thread"
    - path: "frontend-dashboard/src/pages/GoldenThreadPage.tsx"
      provides: "Read-only IDP-to-SDBIP golden thread tree view"
      min_lines: 80
    - path: "frontend-dashboard/src/pages/IdpPage.tsx"
      provides: "IDP cycle list with create form"
      min_lines: 60
    - path: "frontend-dashboard/src/pages/IdpDetailPage.tsx"
      provides: "IDP cycle detail page with goals and objectives management"
      min_lines: 60
    - path: "frontend-dashboard/src/pages/SdbipPage.tsx"
      provides: "SDBIP scorecard list with create form"
      min_lines: 60
    - path: "frontend-dashboard/src/pages/SdbipKpiPage.tsx"
      provides: "KPI list for a scorecard with quarterly targets"
      min_lines: 80
    - path: "frontend-dashboard/src/pages/ActualsPage.tsx"
      provides: "Quarterly actuals submission table with traffic-light badges"
      min_lines: 80
    - path: "frontend-dashboard/src/components/pms/TrafficLightBadge.tsx"
      provides: "Reusable traffic-light status badge component"
      min_lines: 20
  key_links:
    - from: "frontend-dashboard/src/App.tsx"
      to: "frontend-dashboard/src/pages/IdpPage.tsx"
      via: "React Router route /pms/idp"
      pattern: "Route.*pms/idp"
    - from: "frontend-dashboard/src/pages/GoldenThreadPage.tsx"
      to: "src/api/v1/idp.py"
      via: "GET /api/v1/idp/cycles/{id}/golden-thread API call"
      pattern: "golden.thread"
    - from: "frontend-dashboard/src/components/layout/Sidebar.tsx"
      to: "frontend-dashboard/src/hooks/useRoleBasedNav.ts"
      via: "PMS navigation items for authorized roles"
      pattern: "pms|idp|sdbip"
---

<objective>
Create the IDP and SDBIP frontend pages — golden thread view, IDP management, SDBIP scorecard/KPI management, quarterly actuals submission with traffic-light badges, and evidence upload form. Add the golden thread API endpoint.

Purpose: Frontend pages make the entire Phase 28 data backbone usable by municipal staff. The golden thread view is the capstone — showing the full hierarchy from IDP strategic objectives down to operational KPIs, which is the core value proposition of the PMS system.

Output: 7 new React pages, 1 shared component, golden thread API endpoint, updated sidebar navigation.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-idp-sdbip-core-performance-monitoring/28-RESEARCH.md
@.planning/phases/28-idp-sdbip-core-performance-monitoring/28-01-SUMMARY.md
@.planning/phases/28-idp-sdbip-core-performance-monitoring/28-02-SUMMARY.md
@.planning/phases/28-idp-sdbip-core-performance-monitoring/28-04-SUMMARY.md
@.planning/phases/28-idp-sdbip-core-performance-monitoring/28-05-SUMMARY.md
@frontend-dashboard/src/hooks/useRoleBasedNav.ts
@frontend-dashboard/src/components/layout/Sidebar.tsx
@frontend-dashboard/src/App.tsx
@shared/components/ui/GlassCard.tsx
@shared/components/ui/Button.tsx

<interfaces>
<!-- Project decision: CSS variables over Tailwind for all dashboard frontend components -->
<!-- Use inline styles with design-tokens.css variables and @shared/components -->

From shared/components/ui (existing):
- GlassCard: Card container component
- Button: Button component with variants
- Input: Input field component

From frontend-dashboard/src/hooks/useAuth.ts:
```typescript
// Provides user, role, isAuthenticated
```

API endpoints to consume:
- GET /api/v1/idp/cycles — list IDP cycles
- POST /api/v1/idp/cycles — create IDP cycle
- GET /api/v1/idp/cycles/{id}/goals — list goals
- POST /api/v1/idp/cycles/{id}/goals — add goal
- GET /api/v1/idp/cycles/{id}/golden-thread — golden thread tree (new in this plan)
- GET /api/v1/sdbip/scorecards — list scorecards
- POST /api/v1/sdbip/scorecards — create scorecard
- GET /api/v1/sdbip/scorecards/{id}/kpis — list KPIs
- POST /api/v1/sdbip/scorecards/{id}/kpis — create KPI
- GET /api/v1/sdbip/kpis/{id}/actuals — list actuals
- POST /api/v1/sdbip/actuals — submit actual
- POST /api/v1/sdbip/actuals/{id}/evidence — upload evidence
- GET /api/v1/sdbip/actuals/{id}/evidence — list evidence
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Golden thread backend API endpoint</name>
  <files>
    src/services/idp_service.py
    src/api/v1/idp.py
  </files>
  <action>
Add `get_golden_thread` method to `IDPService` in `src/services/idp_service.py`:
```python
async def get_golden_thread(self, cycle_id: UUID, db: AsyncSession) -> dict:
    """Return IDP Cycle -> Goals -> Objectives -> SDBIP KPIs tree."""
    from sqlalchemy.orm import selectinload
    result = await db.execute(
        select(IDPCycle)
        .options(
            selectinload(IDPCycle.goals)
            .selectinload(IDPGoal.objectives)
            .selectinload(IDPObjective.sdbip_kpis)
        )
        .where(IDPCycle.id == cycle_id)
    )
    cycle = result.scalar_one_or_none()
    if not cycle:
        raise HTTPException(404, "IDP cycle not found")

    return {
        "id": str(cycle.id),
        "title": cycle.title,
        "status": cycle.status,
        "goals": [
            {
                "id": str(goal.id),
                "title": goal.title,
                "national_kpa": goal.national_kpa,
                "objectives": [
                    {
                        "id": str(obj.id),
                        "title": obj.title,
                        "kpis": [
                            {
                                "id": str(kpi.id),
                                "kpi_number": kpi.kpi_number,
                                "description": kpi.description,
                                "unit_of_measurement": kpi.unit_of_measurement,
                                "annual_target": str(kpi.annual_target),
                            }
                            for kpi in (obj.sdbip_kpis or [])
                        ]
                    }
                    for obj in goal.objectives
                ]
            }
            for goal in cycle.goals
        ]
    }
```

Add endpoint to `src/api/v1/idp.py`:
- `GET /cycles/{cycle_id}/golden-thread` — returns golden thread tree (200). Dependencies: `require_pms_ready()`, `require_min_tier(3)`.
  </action>
  <verify>
    <automated>cd C:/Users/Bantu/mzansi-agentive/salga-trust-engine && python -m pytest tests/test_pms_idp.py -x --tb=short -k "golden_thread" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Golden thread API endpoint returns IDP -> Goals -> Objectives -> KPIs tree structure
    - Uses selectinload for eager loading (no N+1 queries)
    - Returns 404 for non-existent cycle
  </done>
</task>

<task type="auto">
  <name>Task 1b: PMS frontend pages and TrafficLightBadge component</name>
  <files>
    frontend-dashboard/src/pages/IdpPage.tsx
    frontend-dashboard/src/pages/IdpDetailPage.tsx
    frontend-dashboard/src/pages/SdbipPage.tsx
    frontend-dashboard/src/pages/SdbipKpiPage.tsx
    frontend-dashboard/src/pages/ActualsPage.tsx
    frontend-dashboard/src/pages/EvidencePage.tsx
    frontend-dashboard/src/pages/GoldenThreadPage.tsx
    frontend-dashboard/src/components/pms/TrafficLightBadge.tsx
  </files>
  <action>
IMPORTANT: Use CSS variables from `@shared/design-tokens.css` and inline styles. No Tailwind. Use `GlassCard`, `Button`, `Input` from `@shared/components/ui/`.

1. **`frontend-dashboard/src/components/pms/TrafficLightBadge.tsx`**:
   - Props: `{ status: 'green' | 'amber' | 'red', pct: number }`
   - Renders colored badge with percentage value
   - Colors: green -> var(--color-success), amber -> var(--color-warning), red -> var(--color-error)

2. **`frontend-dashboard/src/pages/IdpPage.tsx`**:
   - List IDP cycles in GlassCards (title, start_year-end_year, status badge)
   - "Create IDP Cycle" button opens inline form
   - Create form: title, vision, mission, start_year, end_year
   - Click cycle card navigates to `/pms/idp/{id}`
   - Fetch from `GET /api/v1/idp/cycles`
   - Use the Vite proxy: fetch from `/api/v1/idp/cycles` (not full URL)

3. **`frontend-dashboard/src/pages/IdpDetailPage.tsx`**:
   - Shows cycle details at top
   - Lists goals with national KPA badges
   - Under each goal, lists objectives
   - "Add Goal" and "Add Objective" inline forms
   - "Approve IDP" / "Open Review" transition buttons (conditional on status)
   - Link to golden thread view

4. **`frontend-dashboard/src/pages/SdbipPage.tsx`**:
   - List SDBIP scorecards grouped by financial year
   - "Create Scorecard" form: financial_year, layer (top/departmental), department (if departmental)
   - Click scorecard navigates to `/pms/sdbip/{id}/kpis`

5. **`frontend-dashboard/src/pages/SdbipKpiPage.tsx`**:
   - Lists KPIs for a scorecard in a table/grid (kpi_number, description, unit, baseline, annual_target, weight)
   - "Add KPI" form with all fields including mSCOA code lookup (GET /mscoa-codes?q=...)
   - Quarterly targets section: 4 input fields (Q1-Q4 target values)
   - Each KPI row links to actuals page

6. **`frontend-dashboard/src/pages/ActualsPage.tsx`**:
   - Shows KPI info at top (description, annual_target, unit)
   - Table of quarterly actuals: quarter, actual_value, achievement_pct with TrafficLightBadge, is_validated badge, submitted_by, submitted_at
   - "Submit Actual" form: quarter dropdown, actual_value input
   - "Validate" button for PMS officer role (calls POST /actuals/{id}/validate)
   - "Upload Evidence" button per actual row -> navigates to evidence page

7. **`frontend-dashboard/src/pages/EvidencePage.tsx`**:
   - Shows actual info at top
   - Lists uploaded evidence documents (filename, content_type, file_size, scan_status, uploaded_by)
   - File upload form (drag-and-drop or file picker)
   - Download link for each document (calls GET /evidence/{id}/download)

8. **`frontend-dashboard/src/pages/GoldenThreadPage.tsx`**:
   - Read-only tree view showing IDP Cycle -> Goals -> Objectives -> KPIs
   - Hierarchical indented list or collapsible tree using simple HTML/CSS (no react-d3-tree — too heavy for a read-only list)
   - Each level has a different background shade
   - KPIs show kpi_number, description, annual_target
   - Cycle selector dropdown at top if multiple cycles exist

All pages handle loading states, error states, and empty states. Use `fetch` with auth token from `useAuth()` hook. Show meaningful error messages from API responses.
  </action>
  <verify>
    <automated>cd C:/Users/Bantu/mzansi-agentive/salga-trust-engine/frontend-dashboard && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 7 frontend pages compile without TypeScript errors
    - TrafficLightBadge component renders green/amber/red correctly
    - Pages use CSS variables and @shared components (no Tailwind)
    - All pages handle loading, error, and empty states
  </done>
</task>

<task type="auto">
  <name>Task 2: Sidebar navigation, routing, and PMS section integration</name>
  <files>
    frontend-dashboard/src/hooks/useRoleBasedNav.ts
    frontend-dashboard/src/components/layout/Sidebar.tsx
    frontend-dashboard/src/App.tsx
  </files>
  <action>
Update `frontend-dashboard/src/hooks/useRoleBasedNav.ts`:
- Add PMS navigation section with items:
  - "IDP Management" -> `/pms/idp` (icon: target/compass)
  - "SDBIP Scorecards" -> `/pms/sdbip` (icon: chart)
  - "Golden Thread" -> `/pms/golden-thread` (icon: link/chain)
- Visible to roles: pms_officer, section56_director, department_manager, cfo, municipal_manager, executive_mayor, admin, salga_admin (all tiers 1-3 except oversight-only roles)
- Keep existing navigation sections intact

Update `frontend-dashboard/src/components/layout/Sidebar.tsx`:
- Render PMS section if user has authorized role
- Use the same styling pattern as existing nav sections
- PMS section header: "Performance Management"

Update `frontend-dashboard/src/App.tsx`:
- Add React Router routes for PMS pages:
  - `/pms/idp` -> IdpPage
  - `/pms/idp/:cycleId` -> IdpDetailPage
  - `/pms/sdbip` -> SdbipPage
  - `/pms/sdbip/:scorecardId/kpis` -> SdbipKpiPage
  - `/pms/kpis/:kpiId/actuals` -> ActualsPage
  - `/pms/actuals/:actualId/evidence` -> EvidencePage
  - `/pms/golden-thread` -> GoldenThreadPage
- All routes wrapped in ProtectedRoute (authenticated users only)
- Import all new page components

Ensure Vite proxy is configured for `/api/v1/idp` and `/api/v1/sdbip` routes. Check `vite.config.ts` — the existing proxy for `/api` should cover these automatically. If not, add proxy entries.
  </action>
  <verify>
    <automated>cd C:/Users/Bantu/mzansi-agentive/salga-trust-engine/frontend-dashboard && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <done>
    - PMS navigation section appears in sidebar for authorized roles
    - All PMS routes registered in App.tsx
    - Navigation links work: clicking "IDP Management" navigates to /pms/idp
    - Routes are protected (authenticated only)
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of PMS frontend pages</name>
  <files>frontend-dashboard/src/pages/IdpPage.tsx, frontend-dashboard/src/pages/SdbipPage.tsx, frontend-dashboard/src/pages/GoldenThreadPage.tsx</files>
  <action>
    Human visual verification of all PMS frontend pages built in Tasks 1 and 2.
    7 PMS frontend pages: IDP cycles, IDP detail with goals/objectives, SDBIP scorecards, KPI management, actuals submission with traffic-light badges, evidence upload, and golden thread tree view. Sidebar navigation includes "Performance Management" section.

    Steps to verify:
    1. Start frontend: `cd frontend-dashboard && npm run dev`
    2. Start backend: `uvicorn src.main:app --reload --port 8000`
    3. Log in with a PMS officer or admin account
    4. Verify sidebar shows "Performance Management" section with IDP, SDBIP, Golden Thread links
    5. Navigate to /pms/idp — verify IDP cycle list page loads (may be empty)
    6. Navigate to /pms/sdbip — verify SDBIP scorecard list page loads
    7. Navigate to /pms/golden-thread — verify golden thread page loads with cycle selector
    8. Check that all pages use the design system (CSS variables, GlassCard components)
    9. Check that traffic-light badge renders in green/amber/red colors
    10. Verify no console errors in browser DevTools
  </action>
  <verify>
    <automated>cd C:/Users/Bantu/mzansi-agentive/salga-trust-engine/frontend-dashboard && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Human approves visual appearance of all PMS pages, sidebar navigation works, design system applied correctly, no console errors</done>
</task>

</tasks>

<verification>
- Golden thread API: `curl -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/idp/cycles/{id}/golden-thread` returns tree
- Frontend TypeScript: `cd frontend-dashboard && npx tsc --noEmit` passes
- All PMS routes accessible in browser after login
- Sidebar shows PMS section for authorized roles
</verification>

<success_criteria>
1. Golden thread API returns IDP -> Goals -> Objectives -> KPIs tree structure using selectinload
2. All 7 frontend pages render correctly with design system styling
3. TrafficLightBadge shows green/amber/red based on achievement percentage
4. Sidebar navigation includes PMS section visible to appropriate roles
5. All routes protected by authentication
6. TypeScript compiles without errors
7. Human verification approves visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/28-idp-sdbip-core-performance-monitoring/28-07-SUMMARY.md`
</output>
