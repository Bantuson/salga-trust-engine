---
phase: 34-municipal-onboarding-production-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-dashboard/src/pages/PmsHubPage.tsx
  - frontend-dashboard/src/pages/SALGAAdminDashboardPage.tsx
  - frontend-dashboard/src/components/pms/CreateIdpModal.tsx
  - frontend-dashboard/src/components/pms/CreateSdbipModal.tsx
  - frontend-dashboard/src/components/pms/CreatePaModal.tsx
  - frontend-dashboard/src/components/salga/MunicipalityDetailModal.tsx
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "PmsHubPage IDP create action opens a modal dialog (not inline card expand)"
    - "PmsHubPage SDBIP create action opens a modal dialog (not inline card expand)"
    - "PmsHubPage Performance Agreement create action opens a modal dialog (not inline card expand)"
    - "SALGAAdminDashboardPage municipality performance ranking click opens a modal dialog (not inline row expand)"
  artifacts:
    - path: "frontend-dashboard/src/components/pms/CreateIdpModal.tsx"
      provides: "IDP creation modal following TeamCreateModal pattern"
      contains: "CreateIdpModal"
    - path: "frontend-dashboard/src/components/pms/CreateSdbipModal.tsx"
      provides: "SDBIP creation modal following TeamCreateModal pattern"
      contains: "CreateSdbipModal"
    - path: "frontend-dashboard/src/components/pms/CreatePaModal.tsx"
      provides: "Performance Agreement creation modal following TeamCreateModal pattern"
      contains: "CreatePaModal"
    - path: "frontend-dashboard/src/components/salga/MunicipalityDetailModal.tsx"
      provides: "Municipality detail modal replacing inline row expand"
      contains: "MunicipalityDetailModal"
  key_links:
    - from: "PmsHubPage.tsx"
      to: "CreateIdpModal.tsx"
      via: "showForm state replaced with showCreateModal state"
      pattern: "CreateIdpModal"
    - from: "SALGAAdminDashboardPage.tsx"
      to: "MunicipalityDetailModal.tsx"
      via: "expandedMunicipality replaced with selectedMunicipality + modal"
      pattern: "MunicipalityDetailModal"
---

<objective>
Convert all inline card-expand create actions to modal dialogs. The PmsHubPage's IDP/SDBIP/PA create buttons and the SALGAAdminDashboardPage's municipality ranking drill-down must use the TeamCreateModal pattern.

Purpose: User requires all create/ranking actions to open modals (matching the team modal design), not expand inline cards. This is a UI consistency requirement across the entire dashboard.

Output: 4 new modal components (CreateIdpModal, CreateSdbipModal, CreatePaModal, MunicipalityDetailModal). PmsHubPage and SALGAAdminDashboardPage updated to use modals instead of inline expand.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34-municipal-onboarding-production-readiness/34-RESEARCH.md

@frontend-dashboard/src/components/teams/TeamCreateModal.tsx
@frontend-dashboard/src/pages/PmsHubPage.tsx
@frontend-dashboard/src/pages/SALGAAdminDashboardPage.tsx

<interfaces>
<!-- Canonical modal pattern from TeamCreateModal -->
From frontend-dashboard/src/components/teams/TeamCreateModal.tsx:
```typescript
interface TeamCreateModalProps {
  onClose: () => void;
  onCreated: () => void;
}
export function TeamCreateModal({ onClose, onCreated }: TeamCreateModalProps)

// Overlay styles:
// position: 'fixed', inset: 0, background: 'rgba(0, 0, 0, 0.5)',
// backdropFilter: 'blur(4px)', zIndex: 1000,
// display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '1rem'

// Modal container:
// background: 'var(--glass-pink-frost)', backdropFilter: 'blur(var(--glass-blur-medium))',
// border: '1px solid var(--glass-border)', borderRadius: 'var(--radius-xl)',
// maxWidth: '720px', width: '100%', maxHeight: '85vh',
// display: 'flex', flexDirection: 'column', overflow: 'hidden'

// Key behaviors: body.style.overflow='hidden', Escape closes, overlay click closes
```

From frontend-dashboard/src/pages/PmsHubPage.tsx:
```typescript
// Current pattern: showForm state toggles inline form visibility
const [showForm, setShowForm] = useState(false);
// Create button: onClick={() => setShowForm(prev => !prev)}
// Each view section receives showForm + onToggleForm props
// Views: 'idp-management', 'sdbip-scorecards', 'quarterly-actuals',
//        'performance-agreements', 'statutory-reports', 'evidence'
```

From frontend-dashboard/src/pages/SALGAAdminDashboardPage.tsx:
```typescript
// Current pattern: expandedMunicipality state shows inline detail panel
const [expandedMunicipality, setExpandedMunicipality] = useState<string | null>(null);
// Row click: toggles expanded row with municipality detail
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PMS modal components (IDP, SDBIP, PA) and wire into PmsHubPage</name>
  <files>
    frontend-dashboard/src/components/pms/CreateIdpModal.tsx,
    frontend-dashboard/src/components/pms/CreateSdbipModal.tsx,
    frontend-dashboard/src/components/pms/CreatePaModal.tsx,
    frontend-dashboard/src/pages/PmsHubPage.tsx
  </files>
  <action>
  **Step 1: Create `frontend-dashboard/src/components/pms/` directory** (if not exists).

  **Step 2: Create CreateIdpModal.tsx** following the EXACT TeamCreateModal pattern:

  ```typescript
  interface CreateIdpModalProps {
    onClose: () => void;
    onCreated: () => void;
  }
  export function CreateIdpModal({ onClose, onCreated }: CreateIdpModalProps)
  ```

  Modal structure (copy TeamCreateModal pattern verbatim):
  - **Overlay:** `position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.5)', backdropFilter: 'blur(4px)', zIndex: 1000, display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '1rem'`
  - **Modal container:** `background: 'var(--glass-pink-frost)', backdropFilter: 'blur(var(--glass-blur-medium))', border: '1px solid var(--glass-border)', borderRadius: 'var(--radius-xl)', maxWidth: '720px', width: '100%', maxHeight: '85vh', display: 'flex', flexDirection: 'column', overflow: 'hidden'`
  - **Header:** Sticky top with title "Create IDP Cycle", close (X) button
  - **Body:** Scrollable form with fields:
    - Title (text input)
    - Financial Year Start (number, e.g. 2025)
    - Financial Year End (number, e.g. 2030)
    - Vision (textarea)
    - Mission (textarea)
  - **Footer:** Sticky bottom with Cancel + Create buttons
  - **Behaviors:** useEffect for body overflow hidden, Escape key handler, overlay click handler
  - **Submit:** POST to `/api/v1/idp/cycles` with auth header, then call onCreated()
  - **Error handling:** Try/catch around fetch, show error message in modal body on failure

  **Step 3: Create CreateSdbipModal.tsx** — same pattern as CreateIdpModal:
  - Title: "Create SDBIP Scorecard"
  - Fields: Title (text), Financial Year (number), Layer (dropdown: 'top_layer' | 'departmental'), Department (dropdown, only shown when layer = 'departmental')
  - Submit: POST to `/api/v1/sdbip/scorecards`

  **Step 4: Create CreatePaModal.tsx** — same pattern:
  - Title: "Create Performance Agreement"
  - Fields: Financial Year (number), Section 57 Manager (dropdown/email input), Description (textarea)
  - Submit: POST to `/api/v1/performance-agreements`

  **Step 5: Update PmsHubPage.tsx**
  Replace the inline `showForm` pattern with modal state:

  ```typescript
  // Replace: const [showForm, setShowForm] = useState(false);
  // With:
  const [showCreateModal, setShowCreateModal] = useState(false);
  ```

  - Change the "Create" button's onClick from `setShowForm(prev => !prev)` to `setShowCreateModal(true)`
  - Remove `showForm` and `onToggleForm` props from view section components
  - Add conditional modal rendering based on `activeView`:
    ```typescript
    {showCreateModal && activeView === 'idp-management' && (
      <CreateIdpModal onClose={() => setShowCreateModal(false)} onCreated={() => { setShowCreateModal(false); /* refresh */ }} />
    )}
    {showCreateModal && activeView === 'sdbip-scorecards' && (
      <CreateSdbipModal onClose={() => setShowCreateModal(false)} onCreated={() => { setShowCreateModal(false); }} />
    )}
    {showCreateModal && activeView === 'performance-agreements' && (
      <CreatePaModal onClose={() => setShowCreateModal(false)} onCreated={() => { setShowCreateModal(false); }} />
    )}
    ```
  - Update the create button label to remove the toggle ("Cancel") behavior — always show the createLabel text
  - Views that don't have a create action (quarterly-actuals, statutory-reports, evidence) should hide the create button

  **Step 6:** Update view section components to remove `showForm`/`onToggleForm` props. If the view components have internal inline form logic that was triggered by `showForm`, remove those inline forms — the modal replaces them entirely. If the inline forms contained submit logic, move that to the appropriate modal.

  **Phase 27-03 CSS lock:** All new components use inline styles with CSS variables. No Tailwind.
  </action>
  <verify>
    <automated>cd frontend-dashboard && npm run build:check</automated>
  </verify>
  <done>
  - CreateIdpModal, CreateSdbipModal, CreatePaModal exist as separate component files
  - PmsHubPage create buttons open modals (not inline expand)
  - Each modal follows TeamCreateModal overlay/container/header/body/footer pattern exactly
  - Escape key and overlay click close modals
  - Body scroll locked when modal open
  - TypeScript build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MunicipalityDetailModal and wire into SALGAAdminDashboardPage</name>
  <files>
    frontend-dashboard/src/components/salga/MunicipalityDetailModal.tsx,
    frontend-dashboard/src/pages/SALGAAdminDashboardPage.tsx
  </files>
  <action>
  **Step 1: Create `frontend-dashboard/src/components/salga/` directory** (if not exists).

  **Step 2: Create MunicipalityDetailModal.tsx** following TeamCreateModal pattern:

  ```typescript
  interface MunicipalityData {
    id: string;
    name: string;
    category: string;
    province: string;
    overallScore: number;
    kpiBreakdown: Array<{
      name: string;
      target: number;
      actual: number;
      achievement: number;
      status: 'green' | 'amber' | 'red';
    }>;
    // ... other fields from SALGAAdminDashboardPage's municipality mock data
  }

  interface MunicipalityDetailModalProps {
    municipality: MunicipalityData;
    onClose: () => void;
  }
  export function MunicipalityDetailModal({ municipality, onClose }: MunicipalityDetailModalProps)
  ```

  Modal structure:
  - **Overlay + Container:** Exact same as TeamCreateModal. maxWidth can be '860px' (wider for data-heavy content).
  - **Header:** Municipality name + category badge + close button
  - **Body:** Scrollable content with:
    - Overall performance score (large number + traffic light badge)
    - Province and demarcation code
    - KPI breakdown table: columns for KPI name, target, actual, achievement %, traffic light status
    - The KPI data comes from the existing `municipality` object's mock data that was previously shown in the inline expanded row
  - **Footer:** Close button only (this is a detail view, not a create form)
  - **Behaviors:** Body overflow hidden, Escape key close, overlay click close

  **Step 3: Update SALGAAdminDashboardPage.tsx**

  Replace the inline expand pattern:

  ```typescript
  // Replace:
  const [expandedMunicipality, setExpandedMunicipality] = useState<string | null>(null);

  // With:
  const [selectedMunicipality, setSelectedMunicipality] = useState<MunicipalityData | null>(null);
  ```

  - Change row click handler from `setExpandedMunicipality(...)` to `setSelectedMunicipality(municipalityObject)`
  - Remove the inline expanded row rendering (`isExpanded` conditional content below each row)
  - Add modal rendering at the end of the component:
    ```typescript
    {selectedMunicipality && (
      <MunicipalityDetailModal
        municipality={selectedMunicipality}
        onClose={() => setSelectedMunicipality(null)}
      />
    )}
    ```
  - Add a visual click indicator on table rows (cursor: pointer, hover background)

  **Phase 27-03 CSS lock:** Inline styles with CSS variables only.
  </action>
  <verify>
    <automated>cd frontend-dashboard && npm run build:check</automated>
  </verify>
  <done>
  - MunicipalityDetailModal.tsx exists with TeamCreateModal pattern
  - SALGAAdminDashboardPage municipality ranking click opens modal (not inline expand)
  - Modal shows municipality detail with KPI breakdown table
  - Escape key and overlay click close the modal
  - No inline expanded rows remain in the municipality ranking section
  - TypeScript build passes
  </done>
</task>

</tasks>

<verification>
1. `cd frontend-dashboard && npm run build:check` — TypeScript compilation passes
2. Visual: Navigate to /pms, click "Create" button for IDP view — modal opens with glassmorphism overlay
3. Visual: Navigate to /pms, switch to SDBIP view, click "Create" — SDBIP modal opens
4. Visual: Navigate to /pms, switch to Performance Agreements view, click "Create" — PA modal opens
5. Visual: Navigate to SALGA Admin dashboard, click a municipality row — detail modal opens (not inline expand)
6. All modals: press Escape to close, click overlay to close, body scroll is locked while open
</verification>

<success_criteria>
- 4 new modal components created following TeamCreateModal pattern exactly
- PmsHubPage has zero inline form expand — all creates use modals
- SALGAAdminDashboardPage has zero inline row expand — municipality detail uses modal
- All modals have: glassmorphism overlay, sticky header, scrollable body, sticky footer, Escape close, overlay click close, body scroll lock
- TypeScript build passes
</success_criteria>

<output>
After completion, create `.planning/phases/34-municipal-onboarding-production-readiness/34-02-SUMMARY.md`
</output>
