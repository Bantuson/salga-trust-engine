---
phase: 34.4-public-dashboard-municipal-analytics-enhancement
plan: 01
subsystem: ui
tags: [react, typescript, supabase, public-dashboard, hooks, financial-year]

# Dependency graph
requires:
  - phase: 32-risk-register-public-transparency
    provides: usePublicStats hooks, types/public.ts, mockDashboardData.ts baseline
provides:
  - LeaderboardEntry type exported from types/public.ts
  - mockLeaderboard data with 5 municipalities and rank_delta tracking
  - saFinancialYear.ts utility (getSAFinancialYear, getQuarterBounds, getCurrentSAQuarter)
  - useResponseTimes, useResolutionRates, useCategoryBreakdown extended with financialYear + quarter params
  - TimePeriodControls component with CustomSelect dropdowns for financial year and quarter
affects:
  - 34.4-02 (chart components and leaderboard UI that depend on this data layer)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - SA financial year April-March convention utility with getSAFinancialYear/getCurrentSAQuarter/getQuarterBounds
    - Quarter-aware data filtering: hooks accept optional financialYear+quarter, use getQuarterBounds for date range
    - TimePeriodControls wraps CustomSelect dropdowns with external state (controlled component pattern)

key-files:
  created:
    - frontend-public/src/utils/saFinancialYear.ts
    - frontend-public/src/components/TimePeriodControls.tsx
  modified:
    - frontend-public/src/types/public.ts
    - frontend-public/src/data/mockDashboardData.ts
    - frontend-public/src/hooks/usePublicStats.ts

key-decisions:
  - "getDefaultTimePeriod helper exported from TimePeriodControls for initializing component state in consuming pages"
  - "useCategoryBreakdown query now selects report_date field in addition to category (required for quarter date range filtering)"
  - "useResponseTimes query now selects report_date in addition to response_hours (required for quarter date range filtering)"
  - "TimePeriodControls generates available years dynamically from current date — no hardcoded year list"

patterns-established:
  - "SA FY utility: March 2026 = Q4 of FY 2025/26 (fyStartYear = month < 3 ? year - 1 : year)"
  - "Quarter bounds: Q4 bounds use fyEndYear for Jan-Mar dates (fyStartYear + 1)"
  - "Hook extension: optional financialYear+quarter params added at end of existing param list for backwards compatibility"

requirements-completed: []

# Metrics
duration: 20min
completed: 2026-03-04
---

# Phase 34.4 Plan 01: Public Dashboard Analytics Data Foundation Summary

**SA financial year utility (April-March), LeaderboardEntry type, quarter-aware useResponseTimes/useResolutionRates/useCategoryBreakdown hooks, and TimePeriodControls selector component — all unused heatmap/system summary code deleted**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-03-04T05:22:00Z
- **Completed:** 2026-03-04T05:42:24Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Deleted all unused heatmap/system summary code (3 types, 2 mock exports, 2 hooks, all their imports)
- Added LeaderboardEntry interface and mockLeaderboard with 5 municipalities and previous_rank/rank_delta tracking
- Created saFinancialYear.ts utility with correct April-March convention (March 2026 = Q4 of FY 2025/26)
- Extended useResponseTimes, useResolutionRates, useCategoryBreakdown with optional financialYear + quarter params
- Created TimePeriodControls component using existing CustomSelect with "Reporting Period" label

## Task Commits

Each task was committed atomically:

1. **Task 1: Delete unused code, add LeaderboardEntry type, create SA FY utility, add mock leaderboard data** - `4517913` (feat)
2. **Task 2: Extend hooks with quarter/year filtering, delete unused hooks, create TimePeriodControls component** - `5eed349` (feat)

## Files Created/Modified
- `frontend-public/src/types/public.ts` — Removed HeatmapPoint, SystemSummary, PublicHeatmapRow; added LeaderboardEntry
- `frontend-public/src/data/mockDashboardData.ts` — Removed mockSystemSummary, mockHeatmapData; added mockLeaderboard with 5 entries
- `frontend-public/src/utils/saFinancialYear.ts` — NEW: getSAFinancialYear, getCurrentSAQuarter, getQuarterBounds with April-March convention
- `frontend-public/src/hooks/usePublicStats.ts` — Deleted useHeatmapData and useSystemSummary; extended 3 hooks with financialYear+quarter params
- `frontend-public/src/components/TimePeriodControls.tsx` — NEW: financial year + quarter selector using CustomSelect, with getDefaultTimePeriod helper

## Decisions Made
- `getDefaultTimePeriod` exported alongside `TimePeriodControls` to provide a ready-made initializer for consuming page state — avoids duplicating getSAFinancialYear/getCurrentSAQuarter calls in every page
- `useCategoryBreakdown` query now includes `report_date` field (was `category` only) — required for the quarter date range filter to work; no breaking change since response shape is unchanged
- `useResponseTimes` query now includes `report_date` — same reason as above
- `TimePeriodControls` generates year list dynamically from `new Date()` — no hardcoded years, always shows current and previous FY

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Data layer for Plan 34.4-02 is complete: LeaderboardEntry type, mockLeaderboard, TimePeriodControls component, and quarter-aware hooks are all ready
- Plan 02 can immediately build chart components and leaderboard UI consuming these artifacts
- No blockers

---
*Phase: 34.4-public-dashboard-municipal-analytics-enhancement*
*Completed: 2026-03-04*
