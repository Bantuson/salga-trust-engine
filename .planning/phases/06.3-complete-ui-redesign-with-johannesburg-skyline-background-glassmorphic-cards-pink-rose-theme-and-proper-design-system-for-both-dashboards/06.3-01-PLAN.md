---
phase: 06.3-complete-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/design-tokens.css
  - shared/animations.css
  - shared/components/AnimatedGradientBg.tsx
  - shared/components/ui/GlassCard.tsx
  - shared/components/ui/Skeleton.tsx
autonomous: true

must_haves:
  truths:
    - "All CSS custom properties use pink/rose (#cd5e81) palette instead of navy (#0A0E1A)"
    - "Animated gradient cycles through pink/rose tones (not navy/dark)"
    - "GlassCard default uses white frost + subtle blur, elevated uses pink frost + medium blur"
    - "Skeleton shimmer uses darker pink tones instead of white-on-navy"
    - "GlowPulse animation uses rose tones instead of coral-on-navy"
  artifacts:
    - path: "shared/design-tokens.css"
      provides: "Pink/rose color token system"
      contains: "--surface-base: #cd5e81"
    - path: "shared/components/AnimatedGradientBg.tsx"
      provides: "Pink/rose gradient background"
      contains: "var(--color-rose)"
    - path: "shared/components/ui/GlassCard.tsx"
      provides: "Mixed tint glassmorphic cards"
      contains: "glass-white-frost"
  key_links:
    - from: "shared/design-tokens.css"
      to: "both App.css files"
      via: "@import design-tokens.css"
      pattern: "var\\(--surface-base\\)"
---

<objective>
Replace the entire navy dark mode design token system with a pink/rose (#cd5e81) dominant palette and update all shared components that consume these tokens.

Purpose: This is the foundation layer. Both dashboards import shared/design-tokens.css, so updating tokens here cascades automatically. The AnimatedGradientBg, GlassCard, and Skeleton components need explicit updates because they use hardcoded colors alongside tokens.

Output: Updated design-tokens.css, animations.css, AnimatedGradientBg.tsx, GlassCard.tsx, Skeleton.tsx — all using pink/rose palette.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@shared/design-tokens.css
@shared/animations.css
@shared/components/AnimatedGradientBg.tsx
@shared/components/ui/GlassCard.tsx
@shared/components/ui/Skeleton.tsx
@.planning/phases/06.3-complete-ui-redesign-with-johannesburg-skyline-background-glassmorphic-cards-pink-rose-theme-and-proper-design-system-for-both-dashboards/06.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace navy design tokens with pink/rose palette</name>
  <files>shared/design-tokens.css, shared/animations.css</files>
  <action>
Update shared/design-tokens.css to replace the entire navy palette with pink/rose:

**Core Palette — replace these tokens:**
- `--color-navy: #0A0E1A` → `--color-rose: #cd5e81` (brand primary)
- `--color-navy-light: #131829` → `--color-rose-light: #e68ba5` (lighter rose)
- `--color-navy-surface: #1A1F3A` → `--color-rose-deep: #a34866` (deeper magenta)
- Keep `--color-coral` renamed to `--color-accent-gold: #ffd54f` (gold reads well on pink)
- `--color-coral-hover` → `--color-accent-gold-hover: #ffe082`
- Keep `--color-teal: #00bfa5` (shifted teal for pink readability, was #00D9A6)
- `--color-teal-hover: #00d9b8`
- `--color-gold: #FBBF24` stays

**Text — update for pink background:**
- `--text-primary: #f9f7f8` (light, not full white)
- `--text-secondary: #e0d4d8` (softer secondary on pink)
- `--text-muted: #c7b3ba` (muted on pink)

**Surfaces — update for pink base:**
- `--surface-base: #cd5e81` (primary background)
- `--surface-elevated: rgba(255, 255, 255, 0.08)` (keep white overlay approach)
- `--surface-higher: rgba(255, 255, 255, 0.12)`
- `--surface-highest: rgba(255, 255, 255, 0.16)`

**Borders — keep white-based, adjust opacity:**
- `--border-subtle: rgba(255, 255, 255, 0.15)` (slightly more visible on pink)
- `--border-hover: rgba(255, 255, 255, 0.25)`

**Glow Effects — adapt for pink context:**
- `--glow-coral` → `--glow-gold: 0 0 20px rgba(255, 213, 79, 0.2), 0 0 60px rgba(255, 213, 79, 0.08)`
- `--glow-teal: 0 0 20px rgba(0, 191, 165, 0.2), 0 0 60px rgba(0, 191, 165, 0.08)`
- `--glow-coral-border` → `--glow-gold-border: rgba(255, 213, 79, 0.25)`
- `--glow-teal-border: rgba(0, 191, 165, 0.25)`

**Chart Colors — adapted for pink bg readability:**
- `--chart-primary: #ffd54f` (gold)
- `--chart-secondary: #00bfa5` (teal)
- `--chart-tertiary: #7c4dff` (purple)
- `--chart-quaternary: #ff6e40` (orange)
- `--chart-grid: rgba(255, 255, 255, 0.08)`
- `--chart-axis-text: #e0d4d8`

**Glassmorphism — new tokens:**
- `--glass-bg` → `--glass-white-frost: rgba(255, 255, 255, 0.15)` (default variant)
- `--glass-pink-frost: rgba(205, 94, 129, 0.15)` (elevated variant)
- `--glass-border: rgba(255, 255, 255, 0.2)` (all cards)
- `--glass-blur-subtle: 10px`
- `--glass-blur-medium: 20px`
- Remove old `--glass-blur: 20px` and `--glass-bg`

**Skeleton tokens (new):**
- `--skeleton-base: rgba(163, 72, 102, 0.3)`
- `--skeleton-highlight: rgba(205, 94, 129, 0.5)`

**Gradients — pink/rose tones:**
- `--gradient-1: linear-gradient(135deg, #cd5e81 0%, #e68ba5 50%, #d97491 100%)`
- `--gradient-2: linear-gradient(225deg, #cd5e81 0%, #c25474 50%, #a34866 100%)`
- `--gradient-3: linear-gradient(315deg, #cd5e81 0%, #b84d6f 50%, #e68ba5 100%)`

Keep all font, spacing, radius, and transition tokens unchanged.

Update the doc comment at top to reflect new palette: "Color palette: Rose #cd5e81, Gold #ffd54f, Teal #00bfa5"

In shared/animations.css:
- Update `glowPulse` keyframe: change `rgba(255, 107, 74, ...)` to `rgba(255, 213, 79, ...)` (gold glow on pink)
- All other keyframes (fadeIn, slideUp, gradientShift, float, spin, gradientFadeInOut) are color-agnostic — leave unchanged
  </action>
  <verify>
Open shared/design-tokens.css and confirm:
1. No references to `#0A0E1A`, `#131829`, `#1A1F3A` remain
2. `--surface-base` is `#cd5e81`
3. All three gradient vars use pink/rose tones
4. Glass tokens include `--glass-white-frost` and `--glass-pink-frost`
  </verify>
  <done>All CSS custom properties use pink/rose palette. No navy colors remain in shared token files.</done>
</task>

<task type="auto">
  <name>Task 2: Update AnimatedGradientBg, GlassCard, and Skeleton components</name>
  <files>shared/components/AnimatedGradientBg.tsx, shared/components/ui/GlassCard.tsx, shared/components/ui/Skeleton.tsx</files>
  <action>
**AnimatedGradientBg.tsx:**
- In the reduced motion fallback, change `background: 'var(--color-navy)'` to `background: 'var(--color-rose)'`
- The gradient layers already use `var(--gradient-1/2/3)` which will update automatically from tokens
- No other changes needed

**GlassCard.tsx:**
- Update `variantStyles` object with new glassmorphism per user decisions:
  - `default`: `background: 'var(--glass-white-frost)'`, `backdropFilter: 'blur(var(--glass-blur-subtle))'`, `WebkitBackdropFilter: 'blur(var(--glass-blur-subtle))'`, `border: '1px solid var(--glass-border)'`
  - `elevated`: `background: 'var(--glass-pink-frost)'`, `backdropFilter: 'blur(var(--glass-blur-medium))'`, `WebkitBackdropFilter: 'blur(var(--glass-blur-medium))'`, `border: '1px solid var(--glass-border)'`
  - `interactive`: `background: 'var(--glass-white-frost)'`, `backdropFilter: 'blur(var(--glass-blur-subtle))'`, `WebkitBackdropFilter: 'blur(var(--glass-blur-subtle))'`, `border: '1px solid var(--glass-border)'`, `cursor: 'pointer'`
- Update `glowStyles` — replace coral glow with gold glow:
  - `coral` key → rename to `gold`: `boxShadow: '0 0 20px rgba(255, 213, 79, 0.2), 0 0 60px rgba(255, 213, 79, 0.08)'`
  - `teal`: `boxShadow: '0 0 20px rgba(0, 191, 165, 0.2), 0 0 60px rgba(0, 191, 165, 0.08)'`
  - `gold`: same as above gold
  - Keep `none: {}`
- Update `GlowColor` type: `'gold' | 'teal' | 'none'` (remove 'coral', keep backward compat by making 'gold' the default accent)
- Update hover borderColor in `combinedStyles`: change `rgba(255, 255, 255, 0.2)` to `var(--glass-border)` for consistency

**Skeleton.tsx:**
- Update `darkModeColors` object:
  - `baseColor: 'rgba(163, 72, 102, 0.3)'` (darker pink)
  - `highlightColor: 'rgba(205, 94, 129, 0.5)'` (primary pink shimmer)
- Rename the const from `darkModeColors` to `themeColors` since it's no longer dark mode
  </action>
  <verify>
1. Read AnimatedGradientBg.tsx and confirm no references to `--color-navy` remain
2. Read GlassCard.tsx and confirm variant styles use `--glass-white-frost` / `--glass-pink-frost` / `--glass-blur-subtle` / `--glass-blur-medium`
3. Read Skeleton.tsx and confirm base/highlight colors use pink tones
  </verify>
  <done>All three shared components use pink/rose tokens and glassmorphism variants. No hardcoded navy colors remain in shared components.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "0A0E1A\|131829\|1A1F3A\|color-navy" shared/` returns zero matches
2. `grep "glass-white-frost\|glass-pink-frost" shared/design-tokens.css` returns matches
3. `grep "surface-base.*cd5e81" shared/design-tokens.css` returns a match
</verification>

<success_criteria>
- shared/design-tokens.css has complete pink/rose palette with zero navy references
- AnimatedGradientBg uses pink gradient tokens
- GlassCard implements mixed tint (white frost default, pink frost elevated) with mixed blur (subtle/medium)
- Skeleton uses pink shimmer colors
- Both App.css files will inherit new tokens automatically via @import
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-complete-ui-redesign-with-johannesburg-skyline-background-glassmorphic-cards-pink-rose-theme-and-proper-design-system-for-both-dashboards/06.3-01-SUMMARY.md`
</output>
