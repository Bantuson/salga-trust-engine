---
phase: 34.1-demo-mode-fetch-fixes-for-idp-and-sdbip-pages
verified: 2026-03-03T22:10:00Z
status: human_needed
score: 4/4 must-haves verified
human_verification:
  - test: "Navigate to an IDP cycle card in demo mode (VITE_DEMO_MODE=true npm run dev) and click through to the detail page"
    expected: "IdpDetailPage renders immediately showing 'Umsobomvu IDP 2022-2027', 3 strategic goals, and 4 objectives — no 'Loading IDP cycle...' flash, no error state"
    why_human: "Loading-flash prevention requires visual observation across multiple render frames; cannot be verified by static code analysis alone"
  - test: "Click a KPI 'Actuals' button in demo mode to navigate to /pms/kpis/:kpiId/actuals"
    expected: "ActualsPage renders immediately showing KPI-BSD-001 header and a table with 2 rows (Q1 red validated, Q2 amber auto) — no 'Loading actuals...' flash"
    why_human: "Same as above — loading-flash elimination requires live browser observation"
  - test: "Click an 'Evidence' button from the actuals table in demo mode to navigate to /pms/actuals/:actualId/evidence"
    expected: "EvidencePage renders immediately showing Q1/2025/26 actual info and a document list with 2 files — no 'Loading evidence documents...' flash"
    why_human: "Same as above"
  - test: "In demo mode on IdpDetailPage, click 'Approve IDP', 'Open Review', 'Add Goal' (submit form), and '+ Add Objective' (submit form)"
    expected: "Nothing happens — no fetch errors in browser console, no error state shown, forms close silently or stay open"
    why_human: "Console error absence and silent no-op mutation behaviour cannot be verified by static analysis"
---

# Phase 34.1: Demo Mode Fetch Fixes for IDP and SDBIP Pages — Verification Report

**Phase Goal:** Add DEMO_MODE guards to IdpDetailPage, ActualsPage, and EvidencePage so they render demo data immediately without loading flash or error state in demo mode.
**Verified:** 2026-03-03T22:10:00Z
**Status:** human_needed (all automated checks passed; visual flash-prevention requires browser confirmation)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Clicking an IDP cycle card in demo mode loads IdpDetailPage with demo cycle header, goals, and objectives — no loading flash, no error state | VERIFIED (automated) + NEEDS HUMAN (visual flash) | `DEMO_MODE` guard at line 126, before `setLoading(true)` at line 133; `setLoading(false)` called at line 130 in demo branch; all three state setters called (`setCycle`, `setGoals`, `setObjectivesByGoal`) |
| 2 | Clicking a KPI 'Actuals' button in demo mode loads ActualsPage with demo KPI info and quarterly actuals table — no loading flash, no error state | VERIFIED (automated) + NEEDS HUMAN (visual flash) | `DEMO_MODE` guard at line 111, before `setLoading(true)` at line 117; `setLoading(false)` at line 114; `setKpi` and `setActuals` called with 2-row demo data |
| 3 | Clicking an 'Evidence' button in demo mode loads EvidencePage with demo actual info and document list — no loading flash, no error state | VERIFIED (automated) + NEEDS HUMAN (visual flash) | `DEMO_MODE` guard at line 100, before `setLoading(true)` at line 106; `setLoading(false)` at line 103; `setActual` and `setDocuments` called with 2-document demo data |
| 4 | Mutation handlers (handleTransition, handleAddGoal, handleAddObjective, handleSubmitActual, handleValidate, handleUpload, handleDownload) are no-ops in demo mode — no fetch errors in console | VERIFIED (automated) + NEEDS HUMAN (console) | All 7 handlers confirmed to have `if (DEMO_MODE) return;` guards; fetch paths not reachable in demo mode |

**Score:** 4/4 truths structurally verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend-dashboard/src/pages/IdpDetailPage.tsx` | DEMO_MODE guard in fetchAll + mutation guards + demo data constants | VERIFIED | Import at line 16; DEMO_IDP_CYCLE (line 71), DEMO_IDP_GOALS (line 81), DEMO_OBJECTIVES_BY_GOAL (line 87); fetchAll guard at line 126; handleTransition guard at line 177; handleAddGoal guard at line 201; handleAddObjective guard at line 231 |
| `frontend-dashboard/src/pages/ActualsPage.tsx` | DEMO_MODE guard in fetchAll + mutation guards + demo data constants | VERIFIED | Import at line 17; DEMO_KPI (line 53), DEMO_ACTUALS (line 61); fetchAll guard at line 111; handleSubmitActual guard at line 149; handleValidate guard at line 182 |
| `frontend-dashboard/src/pages/EvidencePage.tsx` | DEMO_MODE guard in fetchAll + mutation guards + demo data constants | VERIFIED | Import at line 15; DEMO_ACTUAL (line 55), DEMO_DOCUMENTS (line 62); fetchAll guard at line 100; handleUpload guard at line 137; handleDownload guard at line 180 |
| `frontend-dashboard/src/lib/demoMode.ts` | Export DEMO_MODE constant | VERIFIED | `export const DEMO_MODE = import.meta.env.VITE_DEMO_MODE === 'true';` — unchanged, correct |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `IdpDetailPage.tsx` | `frontend-dashboard/src/lib/demoMode.ts` | `import { DEMO_MODE }` | WIRED | Line 16: `import { DEMO_MODE } from '../lib/demoMode';` confirmed |
| `ActualsPage.tsx` | `frontend-dashboard/src/lib/demoMode.ts` | `import { DEMO_MODE }` | WIRED | Line 17: `import { DEMO_MODE } from '../lib/demoMode';` confirmed |
| `EvidencePage.tsx` | `frontend-dashboard/src/lib/demoMode.ts` | `import { DEMO_MODE }` | WIRED | Line 15: `import { DEMO_MODE } from '../lib/demoMode';` confirmed |

---

## Guard Ordering Verification (Critical Anti-Pattern Check)

The plan explicitly required the DEMO_MODE guard to appear BEFORE `setLoading(true)` in each `fetchAll` to prevent a one-frame loading flash. Initial state is `useState(true)` so `setLoading(false)` must also be called in the demo branch.

| File | DEMO_MODE guard line | setLoading(false) in demo branch line | setLoading(true) (real path) line | Ordering correct? |
|------|---------------------|--------------------------------------|-----------------------------------|-------------------|
| IdpDetailPage.tsx | 126 | 130 | 133 | YES — guard precedes setLoading(true) |
| ActualsPage.tsx | 111 | 114 | 117 | YES — guard precedes setLoading(true) |
| EvidencePage.tsx | 100 | 103 | 106 | YES — guard precedes setLoading(true) |

---

## Mutation Handler Coverage

| File | Handler | Guard Present | Guard Position |
|------|---------|---------------|----------------|
| IdpDetailPage.tsx | `handleTransition` | YES (`if (DEMO_MODE) return;` line 177) | After `if (!cycleId) return;` — preserves param validation |
| IdpDetailPage.tsx | `handleAddGoal` | YES (`if (DEMO_MODE) return;` line 201) | After `e.preventDefault()` and `if (!cycleId) return;` |
| IdpDetailPage.tsx | `handleAddObjective` | YES (`if (DEMO_MODE) return;` line 231) | After `e.preventDefault()` |
| ActualsPage.tsx | `handleSubmitActual` | YES (`if (DEMO_MODE) return;` line 149) | After `e.preventDefault()` |
| ActualsPage.tsx | `handleValidate` | YES (`if (DEMO_MODE) return;` line 182) | First line of handler body |
| EvidencePage.tsx | `handleUpload` | YES (`if (DEMO_MODE) return;` line 137) | First line of handler body |
| EvidencePage.tsx | `handleDownload` | YES (`if (DEMO_MODE) return;` line 180) | First line of handler body |

All 7 mutation handlers documented in the plan are guarded. No gaps.

---

## Demo Data Content Verification

### IdpDetailPage.tsx
- `DEMO_IDP_CYCLE`: Umsobomvu IDP 2022-2027, status 'approved', vision and mission fields populated — matches `IDPCycle` interface
- `DEMO_IDP_GOALS`: 3 goals with distinct `national_kpa` values (basic_service_delivery, local_economic_development, municipal_financial_viability) — matches `IDPGoal` interface
- `DEMO_OBJECTIVES_BY_GOAL`: Keyed Record with 4 total objectives across 3 goals, each with correct `goal_id` back-references — matches `Record<string, IDPObjective[]>` interface

### ActualsPage.tsx
- `DEMO_KPI`: KPI-BSD-001, unit 'percentage', annual_target '12' — matches `SDBIPKpi` interface
- `DEMO_ACTUALS`: 2 rows — Q1 red/validated/not-auto and Q2 amber/not-validated/auto-populated — matches `SDBIPActual` interface; provides meaningful traffic-light variety for demo

### EvidencePage.tsx
- `DEMO_ACTUAL`: Q1/2025-26/3.2 — matches `ActualInfo` interface
- `DEMO_DOCUMENTS`: 2 documents (PDF and JPEG), both scan_status 'clean', with realistic filenames using SA municipal context — matches `EvidenceDocument` interface

---

## Requirements Coverage

Phase 34.1 declared `requirements: []` in plan frontmatter. No requirement IDs were claimed. REQUIREMENTS.md contains no entries mapped to phase 34.1 and no entries referencing demo mode. No orphaned requirements found.

**Requirements status:** N/A — gap-closure phase with no formal requirements.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| IdpDetailPage.tsx | 402, 428, 486 | `placeholder="..."` in form inputs | Info | These are HTML input placeholder attributes on form fields — not stub code. No impact. |
| ActualsPage.tsx | 342, 350 | `placeholder="..."` in form inputs | Info | Same — HTML input placeholder attributes. No impact. |

No blockers. No warnings. The "placeholder" matches are HTML form field hint text, not implementation placeholders.

---

## Commit Verification

Both task commits documented in SUMMARY.md are confirmed in git log:

- `5ed8e11` — `feat(34.1-01): add DEMO_MODE guards and demo data to IdpDetailPage` — modifies `IdpDetailPage.tsx` (+41 lines)
- `ea196b0` — `feat(34.1-01): add DEMO_MODE guards and demo data to ActualsPage and EvidencePage` — modifies both `ActualsPage.tsx` (+43 lines) and `EvidencePage.tsx` (+38 lines)

---

## Human Verification Required

All automated structural checks passed. The remaining verification items require a live browser with `VITE_DEMO_MODE=true`:

### 1. IdpDetailPage: No Loading Flash

**Test:** Start `cd frontend-dashboard && VITE_DEMO_MODE=true npm run dev`, navigate to the PMS Hub, click any IDP cycle card.
**Expected:** Page transitions directly to IdpDetailPage showing "Umsobomvu IDP 2022-2027" with 3 goal cards and 4 objective bullets — the "Loading IDP cycle..." text never appears even briefly.
**Why human:** A one-frame loading flash is below the resolution of static code analysis. Although the guard is structurally placed before `setLoading(true)`, only a browser render confirms the initial `useState(true)` is resolved before paint.

### 2. ActualsPage: No Loading Flash

**Test:** From SdbipKpiPage, click the "Actuals" button on KPI-BSD-001.
**Expected:** ActualsPage renders immediately with the KPI header card and a table showing 2 actuals rows (Q1 Jul-Sep with red traffic light, Q2 Oct-Dec with amber) — "Loading actuals..." never appears.
**Why human:** Same render-frame reason.

### 3. EvidencePage: No Loading Flash

**Test:** From the actuals table, click the "Evidence" button on any row.
**Expected:** EvidencePage renders immediately with "Portfolio of Evidence" header showing Q1/2025/26 actual info and a document list with 2 files — "Loading evidence documents..." never appears.
**Why human:** Same render-frame reason.

### 4. Mutation No-Ops in Demo Mode

**Test:** On IdpDetailPage in demo mode, click "Open Review" (transition button) and the "+ Add Goal" button (submit a goal form).
**Expected:** No network errors in the browser console DevTools Network tab, no error message appears on page, page state remains unchanged.
**Why human:** Console error absence cannot be verified by static analysis; requires DevTools observation.

---

## Summary

All 4 observable truths are structurally satisfied in the codebase:

1. All three files (`IdpDetailPage.tsx`, `ActualsPage.tsx`, `EvidencePage.tsx`) import `DEMO_MODE` from `../lib/demoMode`.
2. Each `fetchAll` has the DEMO_MODE early-return guard placed before `setLoading(true)` with `setLoading(false)` explicitly called in the demo branch — the critical ordering the plan required to prevent loading flash.
3. All 7 mutation handlers across the three files have `if (DEMO_MODE) return;` guards that prevent fetch calls in demo mode.
4. Demo data constants are defined with realistic South African municipal data matching the TypeScript interfaces of each page.
5. No requirement IDs were claimed and none are orphaned in REQUIREMENTS.md.
6. No blocker or warning anti-patterns found. Only HTML input placeholder attributes (info severity).
7. Both task commits (`5ed8e11`, `ea196b0`) are present in the git log.

The phase goal is achieved at the code level. Human smoke-testing in a live browser with `VITE_DEMO_MODE=true` is the remaining gate to confirm no frame-level loading flash occurs during page transitions.

---

_Verified: 2026-03-03T22:10:00Z_
_Verifier: Claude (gsd-verifier)_
